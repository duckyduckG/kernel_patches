From fcdc80f7c2497c18ff1f3f6f862eb8fbb628d8c2 Mon Sep 17 00:00:00 2001
From: Davide Garberi <dade.garberi@gmail.com>
Date: Thu, 5 Mar 2020 18:59:15 +0100
Subject: [PATCH 118/178] input: touchscreen: nvt_touch_*: Nuke wakelock

* Deprecated

Change-Id: I8040d5ab0ee58178c82242bb4cff57bf5ddabaf3
---
 drivers/input/touchscreen/nvt_touch_a/nt36xxx.c   | 7 +++----
 drivers/input/touchscreen/nvt_touch_d2s/nt36xxx.c | 7 +++----
 drivers/input/touchscreen/nvt_touch_e7s/nt36xxx.c | 7 +++----
 drivers/input/touchscreen/nvt_touch_f7a/nt36xxx.c | 7 +++----
 4 files changed, 12 insertions(+), 16 deletions(-)

diff --git a/drivers/input/touchscreen/nvt_touch_a/nt36xxx.c b/drivers/input/touchscreen/nvt_touch_a/nt36xxx.c
index c34b749624d2..d13e1a86bc8d 100644
--- a/drivers/input/touchscreen/nvt_touch_a/nt36xxx.c
+++ b/drivers/input/touchscreen/nvt_touch_a/nt36xxx.c
@@ -25,7 +25,6 @@
 #include <linux/proc_fs.h>
 #include <asm/uaccess.h>
 #include <linux/input/mt.h>
-#include <linux/wakelock.h>
 #include <linux/of_gpio.h>
 #include <linux/of_irq.h>
 
@@ -639,7 +638,7 @@ static int32_t nvt_flash_proc_init(void)
 /* function page definition */
 #define FUNCPAGE_GESTURE         1
 
-static struct wake_lock gestrue_wakelock;
+static struct wakeup_source gesture_wakeup;
 
 /*******************************************************
 Description:
@@ -1018,7 +1017,7 @@ static irqreturn_t nvt_ts_irq_handler(int32_t irq, void *dev_id)
 
 #if WAKEUP_GESTURE
 	if (bTouchIsAwake == 0) {
-		wake_lock_timeout(&gestrue_wakelock, msecs_to_jiffies(5000));
+		__pm_wakeup_event(&gesture_wakeup, msecs_to_jiffies(5000));
 	}
 #endif
 
@@ -1327,7 +1326,7 @@ static int32_t nvt_ts_probe(struct i2c_client *client, const struct i2c_device_i
 	for (retry = 0; retry < (sizeof(gesture_key_array) / sizeof(gesture_key_array[0])); retry++) {
 		input_set_capability(ts->input_dev, EV_KEY, gesture_key_array[retry]);
 	}
-	wake_lock_init(&gestrue_wakelock, WAKE_LOCK_SUSPEND, "poll-wake-lock");
+	wakeup_source_init(&gesture_wakeup, "poll-wake-lock");
 #endif
 
 	sprintf(ts->phys, "input/ts");
diff --git a/drivers/input/touchscreen/nvt_touch_d2s/nt36xxx.c b/drivers/input/touchscreen/nvt_touch_d2s/nt36xxx.c
index 44ab33e860b3..a37cee5e9319 100644
--- a/drivers/input/touchscreen/nvt_touch_d2s/nt36xxx.c
+++ b/drivers/input/touchscreen/nvt_touch_d2s/nt36xxx.c
@@ -25,7 +25,6 @@
 #include <linux/proc_fs.h>
 #include <asm/uaccess.h>
 #include <linux/input/mt.h>
-#include <linux/wakelock.h>
 #include <linux/of_gpio.h>
 #include <linux/of_irq.h>
 
@@ -775,7 +774,7 @@ static int32_t nvt_flash_proc_init(void)
 /* function page definition */
 #define FUNCPAGE_GESTURE         1
 
-static struct wake_lock gestrue_wakelock;
+static struct wakeup_source gesture_wakeup;
 
 /*******************************************************
 Description:
@@ -1135,7 +1134,7 @@ static irqreturn_t nvt_ts_irq_handler(int32_t irq, void *dev_id)
 
 #if WAKEUP_GESTURE
 	if (bTouchIsAwake == 0) {
-		wake_lock_timeout(&gestrue_wakelock, msecs_to_jiffies(5000));
+		__pm_wakeup_event(&gesture_wakeup, msecs_to_jiffies(5000));
 	}
 #endif
 
@@ -1363,7 +1362,7 @@ static int32_t nvt_ts_probe(struct i2c_client *client, const struct i2c_device_i
 	for (retry = 0; retry < (sizeof(gesture_key_array) / sizeof(gesture_key_array[0])); retry++) {
 		input_set_capability(ts->input_dev, EV_KEY, gesture_key_array[retry]);
 	}
-	wake_lock_init(&gestrue_wakelock, WAKE_LOCK_SUSPEND, "poll-wake-lock");
+	wakeup_source_init(&gesture_wakeup, "poll-wake-lock");
 #endif
 
 	sprintf(ts->phys, "input/ts");
diff --git a/drivers/input/touchscreen/nvt_touch_e7s/nt36xxx.c b/drivers/input/touchscreen/nvt_touch_e7s/nt36xxx.c
index ba41eca040d5..86e9690a8bae 100644
--- a/drivers/input/touchscreen/nvt_touch_e7s/nt36xxx.c
+++ b/drivers/input/touchscreen/nvt_touch_e7s/nt36xxx.c
@@ -25,7 +25,6 @@
 #include <linux/proc_fs.h>
 #include <asm/uaccess.h>
 #include <linux/input/mt.h>
-#include <linux/wakelock.h>
 #include <linux/of_gpio.h>
 #include <linux/of_irq.h>
 
@@ -778,7 +777,7 @@ static int32_t nvt_flash_proc_init(void)
 /* function page definition */
 #define FUNCPAGE_GESTURE         1
 
-static struct wake_lock gestrue_wakelock;
+static struct wakeup_source gesture_wakeup;
 
 /*******************************************************
 Description:
@@ -1138,7 +1137,7 @@ static irqreturn_t nvt_ts_irq_handler(int32_t irq, void *dev_id)
 
 #if WAKEUP_GESTURE
 	if (bTouchIsAwake == 0) {
-		wake_lock_timeout(&gestrue_wakelock, msecs_to_jiffies(5000));
+		__pm_wakeup_event(&gesture_wakeup, msecs_to_jiffies(5000));
 	}
 #endif
 
@@ -1365,7 +1364,7 @@ static int32_t nvt_ts_probe(struct i2c_client *client, const struct i2c_device_i
 	for (retry = 0; retry < (sizeof(gesture_key_array) / sizeof(gesture_key_array[0])); retry++) {
 		input_set_capability(ts->input_dev, EV_KEY, gesture_key_array[retry]);
 	}
-	wake_lock_init(&gestrue_wakelock, WAKE_LOCK_SUSPEND, "poll-wake-lock");
+	wakeup_source_init(&gesture_wakeup, "poll-wake-lock");
 #endif
 
 	sprintf(ts->phys, "input/ts");
diff --git a/drivers/input/touchscreen/nvt_touch_f7a/nt36xxx.c b/drivers/input/touchscreen/nvt_touch_f7a/nt36xxx.c
index 17559fa1c0a9..41df8683b08d 100644
--- a/drivers/input/touchscreen/nvt_touch_f7a/nt36xxx.c
+++ b/drivers/input/touchscreen/nvt_touch_f7a/nt36xxx.c
@@ -25,7 +25,6 @@
 #include <linux/proc_fs.h>
 #include <asm/uaccess.h>
 #include <linux/input/mt.h>
-#include <linux/wakelock.h>
 #include <linux/of_gpio.h>
 #include <linux/of_irq.h>
 
@@ -657,7 +656,7 @@ static int32_t nvt_flash_proc_init(void)
 /* function page definition */
 #define FUNCPAGE_GESTURE         1
 
-static struct wake_lock gestrue_wakelock;
+static struct wakeup_source gesture_wakeup;
 
 /*******************************************************
 Description:
@@ -1090,7 +1089,7 @@ static irqreturn_t nvt_ts_irq_handler(int32_t irq, void *dev_id)
 
 #if WAKEUP_GESTURE
 	if (bTouchIsAwake == 0) {
-		wake_lock_timeout(&gestrue_wakelock, msecs_to_jiffies(5000));
+		__pm_wakeup_event(&gesture_wakeup, msecs_to_jiffies(5000));
 	}
 #endif
 
@@ -1379,7 +1378,7 @@ static int32_t nvt_ts_probe(struct i2c_client *client, const struct i2c_device_i
 	for (retry = 0; retry < (sizeof(gesture_key_array) / sizeof(gesture_key_array[0])); retry++) {
 		input_set_capability(ts->input_dev, EV_KEY, gesture_key_array[retry]);
 	}
-	wake_lock_init(&gestrue_wakelock, WAKE_LOCK_SUSPEND, "poll-wake-lock");
+	wakeup_source_init(&gesture_wakeup, "poll-wake-lock");
 #endif
 
 	sprintf(ts->phys, "input/ts");
-- 
2.49.0

