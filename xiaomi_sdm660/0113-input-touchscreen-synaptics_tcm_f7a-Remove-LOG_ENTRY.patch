From e8bec74c6b9d530f42e4a5d546c8fc3506edc00e Mon Sep 17 00:00:00 2001
From: Michael Bestas <mkbestas@lineageos.org>
Date: Mon, 20 Feb 2023 01:57:29 +0200
Subject: [PATCH 113/178] input: touchscreen: synaptics_tcm_f7a: Remove
 LOG_ENTRY/LOG_DONE logging

Change-Id: I5fe2a36740ed39a92eb62f318178fd68b3c8ba30
---
 .../synaptics_tcm_f7a/synaptics_tcm_core.c    | 118 +-----------------
 .../synaptics_tcm_f7a/synaptics_tcm_core.h    |  14 ---
 .../synaptics_tcm_f7a/synaptics_tcm_device.c  |  31 +----
 .../synaptics_tcm_diagnostics.c               |  33 +----
 .../synaptics_tcm_f7a/synaptics_tcm_i2c.c     |  20 ---
 .../synaptics_tcm_recovery.c                  |  37 +-----
 .../synaptics_tcm_f7a/synaptics_tcm_reflash.c |  85 +------------
 .../synaptics_tcm_f7a/synaptics_tcm_testing.c |  72 +----------
 .../synaptics_tcm_f7a/synaptics_tcm_touch.c   |  47 +------
 .../synaptics_tcm_zeroflash.c                 |  34 +----
 10 files changed, 24 insertions(+), 467 deletions(-)

diff --git a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_core.c b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_core.c
index 883a6fa25d06..a0d6da496651 100644
--- a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_core.c
+++ b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_core.c
@@ -235,7 +235,6 @@ static ssize_t syna_tcm_sysfs_info_show(struct device *dev,
 	struct kobject *p_kobj;
 	struct syna_tcm_hcd *tcm_hcd;
 
-	LOG_ENTRY();
 	p_kobj = sysfs_dir->parent;
 	p_dev = container_of(p_kobj, struct device, kobj);
 	tcm_hcd = dev_get_drvdata(p_dev);
@@ -356,7 +355,6 @@ static ssize_t syna_tcm_sysfs_info_show(struct device *dev,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -369,7 +367,6 @@ static ssize_t syna_tcm_sysfs_irq_en_store(struct device *dev,
 	struct kobject *p_kobj;
 	struct syna_tcm_hcd *tcm_hcd;
 
-	LOG_ENTRY();
 	p_kobj = sysfs_dir->parent;
 	p_dev = container_of(p_kobj, struct device, kobj);
 	tcm_hcd = dev_get_drvdata(p_dev);
@@ -403,7 +400,6 @@ static ssize_t syna_tcm_sysfs_irq_en_store(struct device *dev,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -417,7 +413,6 @@ static ssize_t syna_tcm_sysfs_reset_store(struct device *dev,
 	struct kobject *p_kobj;
 	struct syna_tcm_hcd *tcm_hcd;
 
-	LOG_ENTRY();
 	p_kobj = sysfs_dir->parent;
 	p_dev = container_of(p_kobj, struct device, kobj);
 	tcm_hcd = dev_get_drvdata(p_dev);
@@ -446,7 +441,6 @@ static ssize_t syna_tcm_sysfs_reset_store(struct device *dev,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -458,7 +452,6 @@ static ssize_t syna_tcm_sysfs_watchdog_store(struct device *dev,
 	struct kobject *p_kobj;
 	struct syna_tcm_hcd *tcm_hcd;
 
-	LOG_ENTRY();
 	p_kobj = sysfs_dir->parent;
 	p_dev = container_of(p_kobj, struct device, kobj);
 	tcm_hcd = dev_get_drvdata(p_dev);
@@ -476,7 +469,6 @@ static ssize_t syna_tcm_sysfs_watchdog_store(struct device *dev,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return count;
 }
 
@@ -510,8 +502,6 @@ int syna_tcm_add_module(struct syna_tcm_module_cb *mod_cb, bool insert)
 {
 	struct syna_tcm_module_handler *mod_handler;
 
-	LOG_ENTRY();
-
 	if (!mod_pool.initialized) {
 		mutex_init(&mod_pool.mutex);
 		INIT_LIST_HEAD(&mod_pool.list);
@@ -548,7 +538,6 @@ int syna_tcm_add_module(struct syna_tcm_module_cb *mod_cb, bool insert)
 	if (mod_pool.queue_work)
 		queue_work(mod_pool.workqueue, &mod_pool.work);
 
-	LOG_DONE();
 	return 0;
 }
 EXPORT_SYMBOL(syna_tcm_add_module);
@@ -559,7 +548,6 @@ static void syna_tcm_module_work(struct work_struct *work)
 	struct syna_tcm_module_handler *tmp_handler;
 	struct syna_tcm_hcd *tcm_hcd = mod_pool.tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&mod_pool.mutex);
 	mod_pool.reconstructing = true;
 
@@ -585,7 +573,6 @@ static void syna_tcm_module_work(struct work_struct *work)
 	mod_pool.reconstructing = false;
 	mutex_unlock(&mod_pool.mutex);
 
-	LOG_DONE();
 	return;
 }
 
@@ -603,7 +590,6 @@ static int syna_tcm_report_notifier(void *data)
 	struct syna_tcm_module_handler *mod_handler;
 	struct syna_tcm_hcd *tcm_hcd = data;
 
-	LOG_ENTRY();
 	sched_setscheduler(current, SCHED_RR, &param);
 
 	set_current_state(TASK_INTERRUPTIBLE);
@@ -632,7 +618,6 @@ static int syna_tcm_report_notifier(void *data)
 		set_current_state(TASK_INTERRUPTIBLE);
 	};
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -650,7 +635,6 @@ static void syna_tcm_dispatch_report(struct syna_tcm_hcd *tcm_hcd)
 {
 	struct syna_tcm_module_handler *mod_handler;
 
-	LOG_ENTRY();
 	LOCK_BUFFER(tcm_hcd->in);
 	LOCK_BUFFER(tcm_hcd->report.buffer);
 
@@ -680,7 +664,6 @@ static void syna_tcm_dispatch_report(struct syna_tcm_hcd *tcm_hcd)
 
 	wake_up_process(tcm_hcd->notifier_thread);
 
-	LOG_DONE();
 	return;
 }
 
@@ -695,7 +678,6 @@ static void syna_tcm_dispatch_response(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	if (atomic_read(&tcm_hcd->command_status) != CMD_BUSY)
 		return;
 
@@ -745,7 +727,6 @@ static void syna_tcm_dispatch_response(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	complete(&response_complete);
 
-	LOG_DONE();
 	return;
 }
 
@@ -765,7 +746,6 @@ static void syna_tcm_dispatch_message(struct syna_tcm_hcd *tcm_hcd)
 	unsigned int payload_length;
 	unsigned int max_write_size;
 
-	LOG_ENTRY();
 	if (tcm_hcd->status_report_code == REPORT_IDENTIFY) {
 		payload_length = tcm_hcd->payload_length;
 
@@ -843,7 +823,6 @@ static void syna_tcm_dispatch_message(struct syna_tcm_hcd *tcm_hcd)
 		syna_tcm_dispatch_response(tcm_hcd);
 	}
 
-	LOG_DONE();
 	return;
 }
 
@@ -868,7 +847,6 @@ static int syna_tcm_continued_read(struct syna_tcm_hcd *tcm_hcd)
 	unsigned int total_length;
 	unsigned int remaining_length;
 
-	LOG_ENTRY();
 	total_length = MESSAGE_HEADER_SIZE + tcm_hcd->payload_length + 1;
 
 	remaining_length = total_length - tcm_hcd->read_length;
@@ -977,7 +955,6 @@ static int syna_tcm_continued_read(struct syna_tcm_hcd *tcm_hcd)
 	UNLOCK_BUFFER(tcm_hcd->temp);
 	UNLOCK_BUFFER(tcm_hcd->in);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1003,7 +980,6 @@ static int syna_tcm_raw_read(struct syna_tcm_hcd *tcm_hcd,
 	unsigned int xfer_length;
 	unsigned int remaining_length;
 
-	LOG_ENTRY();
 	if (length < 2) {
 		LOGE(tcm_hcd->pdev->dev.parent,
 				"Invalid length information\n");
@@ -1101,7 +1077,6 @@ static int syna_tcm_raw_read(struct syna_tcm_hcd *tcm_hcd,
 
 	UNLOCK_BUFFER(tcm_hcd->temp);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1126,7 +1101,6 @@ static int syna_tcm_raw_write(struct syna_tcm_hcd *tcm_hcd,
 	unsigned int xfer_length;
 	unsigned int remaining_length;
 
-	LOG_ENTRY();
 	remaining_length = length;
 
 	/* available chunk space for data = total chunk size minus command
@@ -1192,7 +1166,6 @@ static int syna_tcm_raw_write(struct syna_tcm_hcd *tcm_hcd,
 
 	UNLOCK_BUFFER(tcm_hcd->out);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1215,8 +1188,6 @@ static int syna_tcm_read_message(struct syna_tcm_hcd *tcm_hcd,
 	unsigned int total_length;
 	struct syna_tcm_message_header *header;
 
-	LOG_ENTRY();
-
 	mutex_lock(&tcm_hcd->rw_ctrl_mutex);
 
 	if (in_buf != NULL) {
@@ -1368,7 +1339,6 @@ static int syna_tcm_read_message(struct syna_tcm_hcd *tcm_hcd,
 
 	mutex_unlock(&tcm_hcd->rw_ctrl_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1403,8 +1373,6 @@ static int syna_tcm_write_message(struct syna_tcm_hcd *tcm_hcd,
 	unsigned int remaining_length;
 	unsigned int command_status;
 
-	LOG_ENTRY();
-
 	if (response_code != NULL)
 		*response_code = STATUS_INVALID;
 
@@ -1595,7 +1563,6 @@ static int syna_tcm_write_message(struct syna_tcm_hcd *tcm_hcd,
 
 	mutex_unlock(&tcm_hcd->command_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1603,7 +1570,6 @@ static int syna_tcm_wait_hdl(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	msleep(HOST_DOWNLOAD_WAIT_MS);
 
 	if (!atomic_read(&tcm_hcd->host_downloading))
@@ -1621,7 +1587,6 @@ static int syna_tcm_wait_hdl(struct syna_tcm_hcd *tcm_hcd)
 		retval = 0;
 	}
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1629,7 +1594,6 @@ static void syna_tcm_check_hdl(struct syna_tcm_hcd *tcm_hcd)
 {
 	struct syna_tcm_module_handler *mod_handler;
 
-	LOG_ENTRY();
 	LOCK_BUFFER(tcm_hcd->report.buffer);
 
 	tcm_hcd->report.buffer.buf = NULL;
@@ -1652,13 +1616,11 @@ static void syna_tcm_check_hdl(struct syna_tcm_hcd *tcm_hcd)
 
 	mutex_unlock(&mod_pool.mutex);
 
-	LOG_DONE();
 	return;
 }
 
 static void syna_tcm_update_watchdog(struct syna_tcm_hcd *tcm_hcd, bool en)
 {
-	LOG_ENTRY();
 	cancel_delayed_work_sync(&tcm_hcd->watchdog.work);
 	flush_workqueue(tcm_hcd->watchdog.workqueue);
 
@@ -1675,7 +1637,6 @@ static void syna_tcm_update_watchdog(struct syna_tcm_hcd *tcm_hcd, bool en)
 		tcm_hcd->watchdog.count = 0;
 	}
 
-	LOG_DONE();
 	return;
 }
 
@@ -1691,7 +1652,6 @@ static void syna_tcm_watchdog_work(struct work_struct *work)
 	struct syna_tcm_hcd *tcm_hcd =
 		container_of(watchdog, struct syna_tcm_hcd, watchdog);
 
-	LOG_ENTRY();
 	if (mutex_is_locked(&tcm_hcd->rw_ctrl_mutex))
 		goto exit;
 
@@ -1724,13 +1684,11 @@ static void syna_tcm_watchdog_work(struct work_struct *work)
 			&tcm_hcd->watchdog.work,
 			msecs_to_jiffies(WATCHDOG_DELAY_MS));
 
-	LOG_DONE();
 	return;
 }
 
 static void syna_tcm_glove_control(struct syna_tcm_hcd *tcm_hcd, bool en)
 {
-	LOG_ENTRY();
 	cancel_delayed_work_sync(&tcm_hcd->glove.work);
 	flush_workqueue(tcm_hcd->glove.workqueue);
 
@@ -1742,7 +1700,6 @@ static void syna_tcm_glove_control(struct syna_tcm_hcd *tcm_hcd, bool en)
 
 	}
 
-	LOG_DONE();
 	return;
 }
 
@@ -1778,8 +1735,6 @@ static void syna_tcm_glove_work(struct work_struct *work)
 	struct syna_tcm_hcd *tcm_hcd =
 		container_of(glove, struct syna_tcm_hcd, glove);
 
-	LOG_ENTRY();
-
 	if (!tcm_hcd->glove.keep_runing) {
 		LOGV("disable glove mode\n");
 		if (syna_tcm_disable_glove(tcm_hcd) < 0) {
@@ -1788,7 +1743,6 @@ static void syna_tcm_glove_work(struct work_struct *work)
 		}
 	}
 
-	LOG_DONE();
 	return;
 }
 
@@ -1801,7 +1755,6 @@ static void syna_tcm_polling_work(struct work_struct *work)
 		container_of(delayed_work, struct syna_tcm_hcd,
 				polling_work);
 
-	LOG_ENTRY();
 	if (!tcm_hcd->do_polling)
 		return;
 
@@ -1821,7 +1774,6 @@ static void syna_tcm_polling_work(struct work_struct *work)
 				msecs_to_jiffies(POLLING_DELAY_MS));
 	}
 
-	LOG_DONE();
 	return;
 }
 
@@ -1832,8 +1784,6 @@ static irqreturn_t syna_tcm_isr(int irq, void *data)
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 	static int syna_tcm_isr_err_count = 0;
 
-	LOG_ENTRY();
-
 	if (unlikely(gpio_get_value(bdata->irq_gpio) != bdata->irq_on_state))
 		goto exit;
 
@@ -1866,7 +1816,6 @@ static irqreturn_t syna_tcm_isr(int irq, void *data)
 		}
 	}
 exit:
-	LOG_DONE();
 	return IRQ_HANDLED;
 }
 
@@ -1876,7 +1825,6 @@ static int syna_tcm_enable_irq(struct syna_tcm_hcd *tcm_hcd, bool en, bool ns)
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 	static bool irq_freed = true;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->irq_en_mutex);
 
 	if (en) {
@@ -1958,7 +1906,6 @@ static int syna_tcm_enable_irq(struct syna_tcm_hcd *tcm_hcd, bool en, bool ns)
 
 	mutex_unlock(&tcm_hcd->irq_en_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1968,7 +1915,6 @@ static int syna_tcm_set_gpio(struct syna_tcm_hcd *tcm_hcd, int gpio,
 	int retval;
 	char label[16];
 
-	LOG_ENTRY();
 	if (config) {
 		retval = snprintf(label, 16, "tcm_gpio_%d\n", gpio);
 		if (retval < 0) {
@@ -1999,7 +1945,6 @@ static int syna_tcm_set_gpio(struct syna_tcm_hcd *tcm_hcd, int gpio,
 		gpio_free(gpio);
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -2008,7 +1953,6 @@ static int syna_tcm_config_gpio(struct syna_tcm_hcd *tcm_hcd)
 	int retval;
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 
-	LOG_ENTRY();
 	if (bdata->irq_gpio >= 0) {
 		retval = syna_tcm_set_gpio(tcm_hcd, bdata->irq_gpio,
 				true, 0, 0);
@@ -2044,7 +1988,6 @@ static int syna_tcm_config_gpio(struct syna_tcm_hcd *tcm_hcd)
 		msleep(bdata->power_delay_ms);
 	}
 
-	LOG_DONE();
 	return 0;
 
 err_set_gpio_reset:
@@ -2056,7 +1999,6 @@ static int syna_tcm_config_gpio(struct syna_tcm_hcd *tcm_hcd)
 		syna_tcm_set_gpio(tcm_hcd, bdata->irq_gpio, false, 0, 0);
 
 err_set_gpio_irq:
-	LOG_DONE();
 	return retval;
 }
 
@@ -2068,7 +2010,6 @@ static int syna_tcm_get_app_info(struct syna_tcm_hcd *tcm_hcd)
 	unsigned int resp_length;
 	unsigned int timeout;
 
-	LOG_ENTRY();
 	timeout = APP_STATUS_POLL_TIMEOUT_MS;
 
 	resp_buf = NULL;
@@ -2122,7 +2063,6 @@ static int syna_tcm_get_app_info(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	kfree(resp_buf);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2133,7 +2073,6 @@ static int syna_tcm_get_boot_info(struct syna_tcm_hcd *tcm_hcd)
 	unsigned int resp_buf_size;
 	unsigned int resp_length;
 
-	LOG_ENTRY();
 	resp_buf = NULL;
 	resp_buf_size = 0;
 
@@ -2169,7 +2108,6 @@ static int syna_tcm_get_boot_info(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	kfree(resp_buf);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2181,7 +2119,6 @@ static int syna_tcm_identify(struct syna_tcm_hcd *tcm_hcd, bool id)
 	unsigned int resp_length;
 	unsigned int max_write_size;
 
-	LOG_ENTRY();
 	resp_buf = NULL;
 	resp_buf_size = 0;
 
@@ -2254,7 +2191,6 @@ static int syna_tcm_identify(struct syna_tcm_hcd *tcm_hcd, bool id)
 
 	kfree(resp_buf);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2266,7 +2202,6 @@ static int syna_tcm_run_production_test_firmware(struct syna_tcm_hcd *tcm_hcd)
 	unsigned int resp_buf_size;
 	unsigned int resp_length;
 
-	LOG_ENTRY();
 	retry = true;
 
 	resp_buf = NULL;
@@ -2309,7 +2244,6 @@ static int syna_tcm_run_production_test_firmware(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	kfree(resp_buf);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2321,7 +2255,6 @@ static int syna_tcm_run_application_firmware(struct syna_tcm_hcd *tcm_hcd)
 	unsigned int resp_buf_size;
 	unsigned int resp_length;
 
-	LOG_ENTRY();
 	retry = true;
 
 	resp_buf = NULL;
@@ -2372,7 +2305,6 @@ static int syna_tcm_run_application_firmware(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	kfree(resp_buf);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2383,7 +2315,6 @@ static int syna_tcm_run_bootloader_firmware(struct syna_tcm_hcd *tcm_hcd)
 	unsigned int resp_buf_size;
 	unsigned int resp_length;
 
-	LOG_ENTRY();
 	resp_buf = NULL;
 	resp_buf_size = 0;
 
@@ -2422,7 +2353,6 @@ static int syna_tcm_run_bootloader_firmware(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	kfree(resp_buf);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2431,7 +2361,6 @@ static int syna_tcm_switch_mode(struct syna_tcm_hcd *tcm_hcd,
 {
 	int retval;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->reset_mutex);
 
 	tcm_hcd->update_watchdog(tcm_hcd, false);
@@ -2475,7 +2404,6 @@ static int syna_tcm_switch_mode(struct syna_tcm_hcd *tcm_hcd,
 
 	mutex_unlock(&tcm_hcd->reset_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2488,7 +2416,6 @@ static int syna_tcm_get_dynamic_config(struct syna_tcm_hcd *tcm_hcd,
 	unsigned int resp_buf_size;
 	unsigned int resp_length;
 
-	LOG_ENTRY();
 	resp_buf = NULL;
 	resp_buf_size = 0;
 
@@ -2524,7 +2451,6 @@ static int syna_tcm_get_dynamic_config(struct syna_tcm_hcd *tcm_hcd,
 exit:
 	kfree(resp_buf);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2537,7 +2463,6 @@ static int syna_tcm_set_dynamic_config(struct syna_tcm_hcd *tcm_hcd,
 	unsigned int resp_buf_size;
 	unsigned int resp_length;
 
-	LOG_ENTRY();
 	resp_buf = NULL;
 	resp_buf_size = 0;
 
@@ -2566,7 +2491,6 @@ static int syna_tcm_set_dynamic_config(struct syna_tcm_hcd *tcm_hcd,
 exit:
 	kfree(resp_buf);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2579,7 +2503,6 @@ static int syna_tcm_get_data_location(struct syna_tcm_hcd *tcm_hcd,
 	unsigned int resp_buf_size;
 	unsigned int resp_length;
 
-	LOG_ENTRY();
 	switch (area) {
 	case CUSTOM_LCM:
 		out_buf = LCM_DATA;
@@ -2629,7 +2552,6 @@ static int syna_tcm_get_data_location(struct syna_tcm_hcd *tcm_hcd,
 
 exit:
 	kfree(resp_buf);
-	LOG_DONE();
 
 	return retval;
 }
@@ -2642,7 +2564,6 @@ static int syna_tcm_sleep(struct syna_tcm_hcd *tcm_hcd, bool en)
 	unsigned int resp_buf_size;
 	unsigned int resp_length;
 
-	LOG_ENTRY();
 	command = en ? CMD_ENTER_DEEP_SLEEP : CMD_EXIT_DEEP_SLEEP;
 
 	resp_buf = NULL;
@@ -2671,7 +2592,6 @@ static int syna_tcm_sleep(struct syna_tcm_hcd *tcm_hcd, bool en)
 exit:
 	kfree(resp_buf);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2684,7 +2604,6 @@ static int syna_tcm_reset(struct syna_tcm_hcd *tcm_hcd, bool hw, bool update_wd)
 	struct syna_tcm_module_handler *mod_handler;
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 
-	LOG_ENTRY();
 	resp_buf = NULL;
 	resp_buf_size = 0;
 
@@ -2842,7 +2761,6 @@ static int syna_tcm_reset(struct syna_tcm_hcd *tcm_hcd, bool hw, bool update_wd)
 
 	kfree(resp_buf);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2853,7 +2771,6 @@ static int syna_tcm_rezero(struct syna_tcm_hcd *tcm_hcd)
 	unsigned int resp_buf_size;
 	unsigned int resp_length;
 
-	LOG_ENTRY();
 	resp_buf = NULL;
 	resp_buf_size = 0;
 
@@ -2878,7 +2795,6 @@ static int syna_tcm_rezero(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	kfree(resp_buf);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2892,7 +2808,6 @@ static void syna_tcm_helper_work(struct work_struct *work)
 	struct syna_tcm_hcd *tcm_hcd =
 		container_of(helper, struct syna_tcm_hcd, helper);
 
-	LOG_ENTRY();
 	task = atomic_read(&helper->task);
 
 	switch (task) {
@@ -2934,7 +2849,6 @@ static void syna_tcm_helper_work(struct work_struct *work)
 
 	atomic_set(&helper->task, HELP_NONE);
 
-	LOG_DONE();
 	return;
 }
 
@@ -2944,8 +2858,6 @@ static int syna_tcm_resume(struct device *dev)
 	int retval;
 	struct syna_tcm_module_handler *mod_handler;
 	struct syna_tcm_hcd *tcm_hcd = dev_get_drvdata(dev);
-
-	LOG_ENTRY();
 	if (!tcm_hcd->in_suspend)
 		return 0;
 
@@ -3039,7 +2951,6 @@ static int syna_tcm_resume(struct device *dev)
 exit:
 	tcm_hcd->in_suspend = false;
 	LOGV("Out\n");
-	LOG_DONE();
 	return retval;
 }
 
@@ -3048,7 +2959,6 @@ static int syna_tcm_suspend(struct device *dev)
 	struct syna_tcm_module_handler *mod_handler;
 	struct syna_tcm_hcd *tcm_hcd = dev_get_drvdata(dev);
 
-	LOG_ENTRY();
 	if (tcm_hcd->in_suspend)
 		return 0;
 
@@ -3069,7 +2979,6 @@ static int syna_tcm_suspend(struct device *dev)
 
 	tcm_hcd->in_suspend = true;
 
-	LOG_DONE();
 	return 0;
 }
 #endif
@@ -3082,7 +2991,6 @@ static int syna_tcm_early_suspend(struct device *dev)
 	struct syna_tcm_module_handler *mod_handler;
 	struct syna_tcm_hcd *tcm_hcd = dev_get_drvdata(dev);
 
-	LOG_ENTRY();
 	if (tcm_hcd->in_suspend)
 		return 0;
 
@@ -3126,7 +3034,6 @@ static int syna_tcm_early_suspend(struct device *dev)
 		LOGV("gesture mode, keep TP's irq and power support\n");
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -3139,7 +3046,6 @@ static int syna_tcm_fb_notifier_cb(struct notifier_block *nb,
 	struct syna_tcm_hcd *tcm_hcd =
 		container_of(nb, struct syna_tcm_hcd, fb_notifier);
 
-	LOG_ENTRY();
 	retval = 0;
 
 
@@ -3189,7 +3095,6 @@ static int syna_tcm_fb_notifier_cb(struct notifier_block *nb,
 		}
 	}
 
-	LOG_DONE();
 	return retval;
 }
 #endif
@@ -3200,7 +3105,6 @@ static void do_syna_reset_work(struct work_struct *work)
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = g_tcm_hcd;
 
-	LOG_ENTRY();
 	if(tcm_hcd->reseting) return;
 	tcm_hcd->reseting = true;
 	LOGV("ERROR: TouchPad Interrupt Abnormal! Next : Reset TP IC!\n");
@@ -3215,7 +3119,6 @@ static void do_syna_reset_work(struct work_struct *work)
 
 exit:
 	tcm_hcd->reseting = false;
-	LOG_DONE();
 	return;
 }
 /* add reset work by wanghan end */
@@ -3225,12 +3128,12 @@ static void do_syna_resume_work(struct work_struct *work)
 {
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = g_tcm_hcd;
-	LOG_ENTRY();
+
 	mutex_lock(&tcm_hcd->pm_mutex);
 	retval = syna_tcm_resume(&(tcm_hcd->pdev->dev));
 	if (retval < 0) LOGV("syna_tcm_resume Failed! retval = %d\n", retval);
 	mutex_unlock(&tcm_hcd->pm_mutex);
-	LOG_DONE();
+
 	return;
 }
 /* add resume work by wanghan end */
@@ -3241,7 +3144,6 @@ static int lct_tp_work_node_callback(bool flag)
 	int retval = 0;
 	struct syna_tcm_hcd *tcm_hcd = g_tcm_hcd;
 
-	LOG_ENTRY();
 	if (synaptics_gesture_enable_flag) {
 		LOGV("ERROR: synaptics_gesture=%d!\n", synaptics_gesture_enable_flag);
 		return -1;
@@ -3264,7 +3166,6 @@ static int lct_tp_work_node_callback(bool flag)
 	tcm_hcd->in_suspend = false;
 	mutex_unlock(&tcm_hcd->pm_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 #endif
@@ -3274,7 +3175,6 @@ static int lct_tp_gesture_node_callback(bool flag)
 	int retval = 0;
 	struct syna_tcm_hcd *tcm_hcd = g_tcm_hcd;
 
-	LOG_ENTRY();
 	if (tcm_hcd->in_suspend) {
 		LOGV("ERROR: TP is suspend!\n");
 		return -1;
@@ -3286,7 +3186,7 @@ static int lct_tp_gesture_node_callback(bool flag)
 		synaptics_gesture_enable_flag = false;
 		LOGV("disable gesture mode\n");
 	}
-	LOG_DONE();
+
 	return retval;
 }
 
@@ -3362,7 +3262,6 @@ static int syna_tcm_probe(struct platform_device *pdev)
 	const struct syna_tcm_board_data *bdata;
 	const struct syna_tcm_hw_interface *hw_if;
 
-	LOG_ENTRY();
 	hw_if = pdev->dev.platform_data;
 	if (!hw_if) {
 		LOGE(&pdev->dev,
@@ -3619,7 +3518,6 @@ static int syna_tcm_probe(struct platform_device *pdev)
 
 	syna_tcm_glove_control(tcm_hcd, true);
 
-	LOG_DONE();
 	return 0;
 
 #ifndef KEEP_DRIVER_ON_ERROR
@@ -3702,7 +3600,6 @@ static int syna_tcm_probe(struct platform_device *pdev)
 	lct_syna_verify_flag = false;
 	/* add check F7A LCM by wanghan end */
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -3713,7 +3610,6 @@ static int syna_tcm_remove(struct platform_device *pdev)
 	struct syna_tcm_hcd *tcm_hcd = platform_get_drvdata(pdev);
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 
-	LOG_ENTRY();
 	LOGV("remove touchpad driver ...\n");
 	mutex_lock(&mod_pool.mutex);
 
@@ -3800,7 +3696,6 @@ static int syna_tcm_remove(struct platform_device *pdev)
 
 	kfree(tcm_hcd);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -3831,7 +3726,6 @@ static int lct_syna_set_gpio(int gpio, bool config)
 	int retval;
 	char label[16];
 
-	LOG_ENTRY();
 	if (config) {
 		retval = snprintf(label, 16, "f7a_gpio_%d\n", gpio);
 		if (retval < 0) return retval;
@@ -3843,7 +3737,6 @@ static int lct_syna_set_gpio(int gpio, bool config)
 		gpio_free(gpio);
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -3892,8 +3785,6 @@ static int __init syna_tcm_module_init(void)
 {
 	int retval;
 
-	LOG_ENTRY();
-
 #ifdef DOES_NOT_SUPPORT_SHUTDOWN_CHARGING
 	if (strstr(saved_command_line, "androidboot.mode=charger") != NULL) {
 		LOGV("androidboot.mode=charger, TP doesn't support!\n");
@@ -3927,7 +3818,6 @@ static int __init syna_tcm_module_init(void)
 	}
 	retval = platform_driver_register(&syna_tcm_driver);
 
-	LOG_DONE();
 	return retval;
 
 err:
@@ -3938,13 +3828,11 @@ static int __init syna_tcm_module_init(void)
 
 static void __exit syna_tcm_module_exit(void)
 {
-	LOG_ENTRY();
 	LOGV("exit touchpad driver ...\n");
 	platform_driver_unregister(&syna_tcm_driver);
 
 	syna_tcm_bus_exit();
 
-	LOG_DONE();
 	return;
 }
 
diff --git a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_core.h b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_core.h
index 98b49de2df49..db2e1da6f156 100644
--- a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_core.h
+++ b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_core.h
@@ -73,8 +73,6 @@
 #define LOGW(dev, log, ...) LOGy(dev_warn, dev, log, ##__VA_ARGS__)
 #define LOGE(dev, log, ...) LOGy(dev_err, dev, log, ##__VA_ARGS__)
 
-
-
 #if 1
 #define LOGV(log, ...) \
 	printk(KERN_ERR "[synaptics] %s (line %d): " log, __func__, __LINE__, ##__VA_ARGS__)
@@ -82,18 +80,6 @@
 #define LOGV(log, ...) {}
 #endif
 
-#if 0
-#define LOG_ENTRY() \
-	printk(KERN_WARNING "[synaptics][debug] %s (file %s line %d) Entry.\n", __func__, __FILE__, __LINE__)
-#define LOG_DONE() \
-	printk(KERN_WARNING "[synaptics][debug] %s (file %s line %d) Done.\n", __func__, __FILE__, __LINE__)
-#else
-#define LOG_ENTRY() {}
-#define LOG_DONE() {}
-#endif
-
-
-
 #define INIT_BUFFER(buffer, is_clone) \
 	mutex_init(&buffer.buf_mutex); \
 	buffer.clone = is_clone
diff --git a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_device.c b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_device.c
index 2f6b453c1710..22ab06020073 100644
--- a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_device.c
+++ b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_device.c
@@ -81,7 +81,6 @@ static void device_capture_touch_report(unsigned int count)
 	static unsigned int offset;
 	static unsigned int remaining_size;
 
-	LOG_ENTRY();
 	if (count < 2)
 		return;
 
@@ -167,7 +166,6 @@ static void device_capture_touch_report(unsigned int count)
 exit:
 	UNLOCK_BUFFER(device_hcd->report);
 
-	LOG_DONE();
 	return;
 }
 
@@ -179,7 +177,6 @@ static int device_capture_touch_report_config(unsigned int count)
 	unsigned char *data;
 	struct syna_tcm_hcd *tcm_hcd = device_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (device_hcd->raw_mode) {
 		if (count < 3) {
 			LOGE(tcm_hcd->pdev->dev.parent,
@@ -238,7 +235,6 @@ static int device_capture_touch_report_config(unsigned int count)
 
 	UNLOCK_BUFFER(tcm_hcd->config);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -252,7 +248,6 @@ static int device_ioctl(struct inode *inp, struct file *filp, unsigned int cmd,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = device_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	retval = 0;
@@ -286,7 +281,6 @@ static int device_ioctl(struct inode *inp, struct file *filp, unsigned int cmd,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -301,7 +295,6 @@ static ssize_t device_read(struct file *filp, char __user *buf,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = device_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (count == 0)
 		return 0;
 
@@ -367,7 +360,6 @@ static ssize_t device_read(struct file *filp, char __user *buf,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -377,7 +369,6 @@ static ssize_t device_write(struct file *filp, const char __user *buf,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = device_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (count == 0)
 		return 0;
 
@@ -457,7 +448,6 @@ static ssize_t device_write(struct file *filp, const char __user *buf,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -466,7 +456,6 @@ static int device_open(struct inode *inp, struct file *filp)
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = device_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	if (device_hcd->ref_count < 1) {
@@ -478,7 +467,6 @@ static int device_open(struct inode *inp, struct file *filp)
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -486,7 +474,6 @@ static int device_release(struct inode *inp, struct file *filp)
 {
 	struct syna_tcm_hcd *tcm_hcd = device_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	if (device_hcd->ref_count)
@@ -494,19 +481,16 @@ static int device_release(struct inode *inp, struct file *filp)
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return 0;
 }
 
 static char *device_devnode(struct device *dev, umode_t *mode)
 {
-	LOG_ENTRY();
 	if (!mode)
 		return NULL;
 
 	*mode = (S_IRUSR | S_IWUSR | S_IRGRP | S_IWGRP | S_IROTH | S_IWOTH);
 
-	LOG_DONE();
 	return kasprintf(GFP_KERNEL, "%s/%s", PLATFORM_DRIVER_NAME,
 			dev_name(dev));
 }
@@ -515,7 +499,6 @@ static int device_create_class(void)
 {
 	struct syna_tcm_hcd *tcm_hcd = device_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (device_hcd->class != NULL)
 		return 0;
 
@@ -529,7 +512,6 @@ static int device_create_class(void)
 
 	device_hcd->class->devnode = device_devnode;
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -556,7 +538,6 @@ static int device_init(struct syna_tcm_hcd *tcm_hcd)
 	dev_t dev_num;
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 
-	LOG_ENTRY();
 	device_hcd = kzalloc(sizeof(*device_hcd), GFP_KERNEL);
 	if (!device_hcd) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -636,7 +617,6 @@ static int device_init(struct syna_tcm_hcd *tcm_hcd)
 		}
 	}
 
-	LOG_DONE();
 	return 0;
 
 err_create_device:
@@ -657,13 +637,11 @@ static int device_init(struct syna_tcm_hcd *tcm_hcd)
 	kfree(device_hcd);
 	device_hcd = NULL;
 
-	LOG_DONE();
 	return retval;
 }
 
 static int device_remove(struct syna_tcm_hcd *tcm_hcd)
 {
-	LOG_ENTRY();
 	if (!device_hcd)
 		goto exit;
 
@@ -685,7 +663,6 @@ static int device_remove(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	complete(&device_remove_complete);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -693,13 +670,11 @@ static int device_reset(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	if (!device_hcd) {
 		retval = device_init(tcm_hcd);
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -718,7 +693,7 @@ static struct syna_tcm_module_cb device_module = {
 static int __init device_module_init(void)
 {
 	int retval;
-	LOG_ENTRY();
+
 	/* add check F7A LCM by wanghan start */
 	if(!lct_syna_verify_flag)
 		return -ENODEV;
@@ -728,18 +703,16 @@ static int __init device_module_init(void)
 	if(retval) {
 		LOGV("syna_tcm_add_module failed! retval = %d\n", retval);
 	}
-	LOG_DONE();
+
 	return retval;
 }
 
 static void __exit device_module_exit(void)
 {
-	LOG_ENTRY();
 	syna_tcm_add_module(&device_module, false);
 
 	wait_for_completion(&device_remove_complete);
 
-	LOG_DONE();
 	return;
 }
 
diff --git a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_diagnostics.c b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_diagnostics.c
index f0cb1f6be2c2..a2a1783bcd0d 100644
--- a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_diagnostics.c
+++ b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_diagnostics.c
@@ -97,7 +97,6 @@ static ssize_t diag_sysfs_pid_store(struct device *dev,
 	unsigned int input;
 	struct syna_tcm_hcd *tcm_hcd = diag_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (sscanf(buf, "%u", &input) != 1)
 		return -EINVAL;
 
@@ -121,7 +120,6 @@ static ssize_t diag_sysfs_pid_store(struct device *dev,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -131,7 +129,6 @@ static ssize_t diag_sysfs_size_show(struct device *dev,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = diag_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	if (diag_hcd->state == PING) {
@@ -154,7 +151,6 @@ static ssize_t diag_sysfs_size_show(struct device *dev,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -164,7 +160,6 @@ static ssize_t diag_sysfs_type_store(struct device *dev,
 	unsigned int input;
 	struct syna_tcm_hcd *tcm_hcd = diag_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (sscanf(buf, "%u", &input) != 1)
 		return -EINVAL;
 
@@ -174,7 +169,6 @@ static ssize_t diag_sysfs_type_store(struct device *dev,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return count;
 }
 
@@ -186,7 +180,6 @@ static ssize_t diag_sysfs_rows_show(struct device *dev,
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = diag_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	if (tcm_hcd->id_info.mode != MODE_APPLICATION ||
@@ -203,7 +196,6 @@ static ssize_t diag_sysfs_rows_show(struct device *dev,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -215,7 +207,6 @@ static ssize_t diag_sysfs_cols_show(struct device *dev,
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = diag_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	if (tcm_hcd->id_info.mode != MODE_APPLICATION ||
@@ -232,7 +223,6 @@ static ssize_t diag_sysfs_cols_show(struct device *dev,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -244,7 +234,6 @@ static ssize_t diag_sysfs_hybrid_show(struct device *dev,
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = diag_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	if (tcm_hcd->id_info.mode != MODE_APPLICATION ||
@@ -261,7 +250,6 @@ static ssize_t diag_sysfs_hybrid_show(struct device *dev,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -273,7 +261,6 @@ static ssize_t diag_sysfs_buttons_show(struct device *dev,
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = diag_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	if (tcm_hcd->id_info.mode != MODE_APPLICATION ||
@@ -290,7 +277,6 @@ static ssize_t diag_sysfs_buttons_show(struct device *dev,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -302,7 +288,6 @@ static ssize_t diag_sysfs_data_show(struct file *data_file,
 	unsigned int readlen;
 	struct syna_tcm_hcd *tcm_hcd = diag_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	retval = 0;
@@ -357,7 +342,6 @@ static ssize_t diag_sysfs_data_show(struct file *data_file,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -367,7 +351,6 @@ static void diag_report(void)
 	static enum pingpong_state state = PING;
 	struct syna_tcm_hcd *tcm_hcd = diag_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (state == PING) {
 		LOCK_BUFFER(diag_hcd->ping);
 
@@ -435,7 +418,6 @@ static void diag_report(void)
 	if (diag_hcd->pid)
 		send_sig_info(SIGIO, &diag_hcd->sigio, diag_hcd->task);
 
-	LOG_DONE();
 	return;
 }
 
@@ -444,7 +426,6 @@ static int diag_init(struct syna_tcm_hcd *tcm_hcd)
 	int retval;
 	int idx;
 
-	LOG_ENTRY();
 	diag_hcd = kzalloc(sizeof(*diag_hcd), GFP_KERNEL);
 	if (!diag_hcd) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -488,7 +469,6 @@ static int diag_init(struct syna_tcm_hcd *tcm_hcd)
 		goto err_sysfs_create_bin_file;
 	}
 
-	LOG_DONE();
 	return 0;
 
 err_sysfs_create_bin_file:
@@ -505,7 +485,6 @@ static int diag_init(struct syna_tcm_hcd *tcm_hcd)
 	kfree(diag_hcd);
 	diag_hcd = NULL;
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -513,7 +492,6 @@ static int diag_remove(struct syna_tcm_hcd *tcm_hcd)
 {
 	int idx;
 
-	LOG_ENTRY();
 	if (!diag_hcd)
 		goto exit;
 
@@ -533,20 +511,17 @@ static int diag_remove(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	complete(&diag_remove_complete);
 
-	LOG_DONE();
 	return 0;
 }
 
 static int diag_syncbox(struct syna_tcm_hcd *tcm_hcd)
 {
-	LOG_ENTRY();
 	if (!diag_hcd)
 		return 0;
 
 	if (tcm_hcd->report.id == diag_hcd->report_type)
 		diag_report();
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -554,13 +529,11 @@ static int diag_reset(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	if (!diag_hcd) {
 		retval = diag_init(tcm_hcd);
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -579,7 +552,7 @@ static struct syna_tcm_module_cb diag_module = {
 static int __init diag_module_init(void)
 {
 	int retval;
-	LOG_ENTRY();
+
 	/* add check F7A LCM by wanghan start */
 	if(!lct_syna_verify_flag)
 		return -ENODEV;
@@ -589,18 +562,16 @@ static int __init diag_module_init(void)
 	if(retval) {
 		LOGV("syna_tcm_add_module failed! retval = %d\n", retval);
 	}
-	LOG_DONE();
+
 	return retval;
 }
 
 static void __exit diag_module_exit(void)
 {
-	LOG_ENTRY();
 	syna_tcm_add_module(&diag_module, false);
 
 	wait_for_completion(&diag_remove_complete);
 
-	LOG_DONE();
 	return;
 }
 
diff --git a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_i2c.c b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_i2c.c
index ba47bdea426b..e9c3e20459b6 100644
--- a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_i2c.c
+++ b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_i2c.c
@@ -55,7 +55,6 @@ static int parse_dt(struct device *dev, struct syna_tcm_board_data *bdata)
 	struct device_node *np = dev->of_node;
 	const char *name;
 
-	LOG_ENTRY();
 	prop = of_find_property(np, "synaptics,irq-gpio", NULL);
 	if (prop && prop->length) {
 		bdata->irq_gpio = of_get_named_gpio_flags(np,
@@ -198,7 +197,6 @@ static int parse_dt(struct device *dev, struct syna_tcm_board_data *bdata)
 		bdata->ubl_i2c_addr = 0;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 #endif
@@ -208,7 +206,6 @@ static int syna_tcm_i2c_alloc_mem(struct syna_tcm_hcd *tcm_hcd,
 {
 	struct i2c_client *i2c = to_i2c_client(tcm_hcd->pdev->dev.parent);
 
-	LOG_ENTRY();
 	if (size > buf_size) {
 		if (buf_size)
 			kfree(buf);
@@ -222,7 +219,6 @@ static int syna_tcm_i2c_alloc_mem(struct syna_tcm_hcd *tcm_hcd,
 		buf_size = size;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -236,7 +232,6 @@ static int syna_tcm_i2c_rmi_read(struct syna_tcm_hcd *tcm_hcd,
 	struct i2c_client *i2c = to_i2c_client(tcm_hcd->pdev->dev.parent);
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->io_ctrl_mutex);
 
 	address = (unsigned char)addr;
@@ -271,7 +266,6 @@ static int syna_tcm_i2c_rmi_read(struct syna_tcm_hcd *tcm_hcd,
 exit:
 	mutex_unlock(&tcm_hcd->io_ctrl_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -285,7 +279,6 @@ static int syna_tcm_i2c_rmi_write(struct syna_tcm_hcd *tcm_hcd,
 	struct i2c_client *i2c = to_i2c_client(tcm_hcd->pdev->dev.parent);
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->io_ctrl_mutex);
 
 	byte_count = length + 1;
@@ -334,7 +327,6 @@ static int syna_tcm_i2c_rmi_write(struct syna_tcm_hcd *tcm_hcd,
 exit:
 	mutex_unlock(&tcm_hcd->io_ctrl_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -346,7 +338,6 @@ static int syna_tcm_i2c_read(struct syna_tcm_hcd *tcm_hcd, unsigned char *data,
 	struct i2c_msg msg;
 	struct i2c_client *i2c = to_i2c_client(tcm_hcd->pdev->dev.parent);
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->io_ctrl_mutex);
 
 	msg.addr = i2c->addr;
@@ -374,7 +365,6 @@ static int syna_tcm_i2c_read(struct syna_tcm_hcd *tcm_hcd, unsigned char *data,
 exit:
 	mutex_unlock(&tcm_hcd->io_ctrl_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -386,7 +376,6 @@ static int syna_tcm_i2c_write(struct syna_tcm_hcd *tcm_hcd, unsigned char *data,
 	struct i2c_msg msg;
 	struct i2c_client *i2c = to_i2c_client(tcm_hcd->pdev->dev.parent);
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->io_ctrl_mutex);
 
 	msg.addr = i2c->addr;
@@ -414,7 +403,6 @@ static int syna_tcm_i2c_write(struct syna_tcm_hcd *tcm_hcd, unsigned char *data,
 exit:
 	mutex_unlock(&tcm_hcd->io_ctrl_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -423,7 +411,6 @@ static int syna_tcm_i2c_probe(struct i2c_client *i2c,
 {
 	int retval;
 
-	LOG_ENTRY();
 	LOGV("allocate platform device\n");
 	syna_tcm_i2c_device = platform_device_alloc(PLATFORM_DRIVER_NAME, 0);
 	if (!syna_tcm_i2c_device) {
@@ -463,18 +450,15 @@ static int syna_tcm_i2c_probe(struct i2c_client *i2c,
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
 static int syna_tcm_i2c_remove(struct i2c_client *i2c)
 {
-	LOG_ENTRY();
 	syna_tcm_i2c_device->dev.platform_data = NULL;
 
 	platform_device_unregister(syna_tcm_i2c_device);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -511,12 +495,10 @@ int syna_tcm_bus_init(void)
 {
 	int retval;
 
-	LOG_ENTRY();
 	retval = i2c_add_driver(&syna_tcm_i2c_driver);
 	if(retval) {
 		LOGV("i2c_del_driver failed! retval = %d\n", retval);
 	}
-	LOG_DONE();
 
 	return retval;
 }
@@ -524,11 +506,9 @@ EXPORT_SYMBOL(syna_tcm_bus_init);
 
 void syna_tcm_bus_exit(void)
 {
-	LOG_ENTRY();
 	kfree(buf);
 
 	i2c_del_driver(&syna_tcm_i2c_driver);
-	LOG_DONE();
 
 	return;
 }
diff --git a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_recovery.c b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_recovery.c
index 757a2d5fa417..5c3af22698c3 100644
--- a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_recovery.c
+++ b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_recovery.c
@@ -137,7 +137,6 @@ static ssize_t recovery_sysfs_recovery_store(struct device *dev,
 	unsigned int input;
 	struct syna_tcm_hcd *tcm_hcd = recovery_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (sscanf(buf, "%u", &input) != 1)
 		return -EINVAL;
 
@@ -180,7 +179,6 @@ static ssize_t recovery_sysfs_recovery_store(struct device *dev,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -191,7 +189,6 @@ static ssize_t recovery_sysfs_ihex_store(struct file *data_file,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = recovery_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	retval = secure_memcpy(&recovery_hcd->ihex_buf[pos],
@@ -213,7 +210,6 @@ static ssize_t recovery_sysfs_ihex_store(struct file *data_file,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -224,7 +220,6 @@ static int recovery_device_reset(void)
 	struct syna_tcm_hcd *tcm_hcd = recovery_hcd->tcm_hcd;
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 
-	LOG_ENTRY();
 	command = F35_RESET_COMMAND;
 
 	retval = syna_tcm_rmi_write(tcm_hcd,
@@ -239,7 +234,6 @@ static int recovery_device_reset(void)
 
 	msleep(bdata->reset_delay_ms);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -247,7 +241,6 @@ static int recovery_add_data_entry(unsigned char data)
 {
 	struct syna_tcm_hcd *tcm_hcd = recovery_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (recovery_hcd->data_entries >= DATA_BUF_SIZE) {
 		LOGE(tcm_hcd->pdev->dev.parent,
 				"Reached data buffer size limit\n");
@@ -256,7 +249,6 @@ static int recovery_add_data_entry(unsigned char data)
 
 	recovery_hcd->data_buf[recovery_hcd->data_entries++] = data;
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -266,7 +258,6 @@ static int recovery_add_padding(unsigned int *words)
 	unsigned int padding;
 	struct syna_tcm_hcd *tcm_hcd = recovery_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	padding = (F35_CHUNK_SIZE_WORDS - *words % F35_CHUNK_SIZE_WORDS);
 	padding %= F35_CHUNK_SIZE_WORDS;
 
@@ -289,7 +280,6 @@ static int recovery_add_padding(unsigned int *words)
 		padding--;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -310,7 +300,6 @@ static int recovery_parse_ihex(void)
 	unsigned int record;
 	struct syna_tcm_hcd *tcm_hcd = recovery_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	words = 0;
 
 	offset = 0;
@@ -392,7 +381,6 @@ static int recovery_parse_ihex(void)
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -402,7 +390,6 @@ static int recovery_check_status(void)
 	unsigned char status;
 	struct syna_tcm_hcd *tcm_hcd = recovery_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	retval = syna_tcm_rmi_read(tcm_hcd,
 			recovery_hcd->f35_addr.data_base,
 			&status,
@@ -422,7 +409,6 @@ static int recovery_check_status(void)
 		return -EINVAL;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -436,7 +422,6 @@ static int recovery_write_flash(void)
 	unsigned int entries_to_write;
 	struct syna_tcm_hcd *tcm_hcd = recovery_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	entries_written = 0;
 
 	data_ptr = recovery_hcd->data_buf;
@@ -482,7 +467,6 @@ static int recovery_write_flash(void)
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -495,7 +479,6 @@ static int recovery_poll_erase_completion(void)
 	unsigned int timeout;
 	struct syna_tcm_hcd *tcm_hcd = recovery_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	timeout = F35_ERASE_ALL_WAIT_MS;
 
 	data_base = recovery_hcd->f35_addr.data_base;
@@ -569,7 +552,6 @@ static int recovery_poll_erase_completion(void)
 				"Failed to get erase completion\n");
 	}
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -579,7 +561,6 @@ static int recovery_erase_flash(void)
 	unsigned char command;
 	struct syna_tcm_hcd *tcm_hcd = recovery_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	command = F35_ERASE_ALL_COMMAND;
 
 	retval = syna_tcm_rmi_write(tcm_hcd,
@@ -610,7 +591,6 @@ static int recovery_erase_flash(void)
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -620,7 +600,6 @@ static int recovery_set_up_recovery_mode(void)
 	struct syna_tcm_hcd *tcm_hcd = recovery_hcd->tcm_hcd;
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 
-	LOG_ENTRY();
 	retval = tcm_hcd->identify(tcm_hcd, true);
 	if (retval < 0) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -655,7 +634,6 @@ static int recovery_set_up_recovery_mode(void)
 
 	msleep(bdata->reset_delay_ms);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -665,7 +643,6 @@ static int recovery_do_recovery(void)
 	struct rmi_pdt_entry p_entry;
 	struct syna_tcm_hcd *tcm_hcd = recovery_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	retval = recovery_parse_ihex();
 	if (retval < 0) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -766,7 +743,6 @@ static int recovery_do_recovery(void)
 		}
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -775,7 +751,6 @@ static int recovery_init(struct syna_tcm_hcd *tcm_hcd)
 	int retval;
 	int idx;
 
-	LOG_ENTRY();
 	recovery_hcd = kzalloc(sizeof(*recovery_hcd), GFP_KERNEL);
 	if (!recovery_hcd) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -834,7 +809,6 @@ static int recovery_init(struct syna_tcm_hcd *tcm_hcd)
 		goto err_sysfs_create_bin_file;
 	}
 
-	LOG_DONE();
 	return 0;
 
 err_sysfs_create_bin_file:
@@ -852,7 +826,6 @@ static int recovery_init(struct syna_tcm_hcd *tcm_hcd)
 	kfree(recovery_hcd);
 	recovery_hcd = NULL;
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -860,7 +833,6 @@ static int recovery_remove(struct syna_tcm_hcd *tcm_hcd)
 {
 	int idx;
 
-	LOG_ENTRY();
 	if (!recovery_hcd)
 		goto exit;
 
@@ -883,7 +855,6 @@ static int recovery_remove(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	complete(&recovery_remove_complete);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -891,13 +862,11 @@ static int recovery_reset(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	if (!recovery_hcd) {
 		retval = recovery_init(tcm_hcd);
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -916,7 +885,7 @@ static struct syna_tcm_module_cb recovery_module = {
 static int __init recovery_module_init(void)
 {
 	int retval;
-	LOG_ENTRY();
+
 	/* add check F7A LCM by wanghan start */
 	if(!lct_syna_verify_flag)
 		return -ENODEV;
@@ -926,18 +895,16 @@ static int __init recovery_module_init(void)
 	if(retval) {
 		LOGV("syna_tcm_add_module failed! retval = %d\n", retval);
 	}
-	LOG_DONE();
+
 	return retval;
 }
 
 static void __exit recovery_module_exit(void)
 {
-	LOG_ENTRY();
 	syna_tcm_add_module(&recovery_module, false);
 
 	wait_for_completion(&recovery_remove_complete);
 
-	LOG_DONE();
 	return;
 }
 
diff --git a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_reflash.c b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_reflash.c
index 62e27af15a4a..d6dd45d47e70 100644
--- a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_reflash.c
+++ b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_reflash.c
@@ -426,7 +426,6 @@ static ssize_t reflash_sysfs_reflash_store(struct device *dev,
 	unsigned int input;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (sscanf(buf, "%u", &input) != 1)
 		return -EINVAL;
 
@@ -522,7 +521,6 @@ static ssize_t reflash_sysfs_reflash_store(struct device *dev,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -533,7 +531,6 @@ static ssize_t reflash_sysfs_image_store(struct file *data_file,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	retval = secure_memcpy(&reflash_hcd->image_buf[pos],
@@ -555,7 +552,6 @@ static ssize_t reflash_sysfs_image_store(struct file *data_file,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -567,7 +563,6 @@ static ssize_t reflash_sysfs_lockdown_show(struct file *data_file,
 	unsigned int readlen;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	mutex_lock(&reflash_hcd->reflash_mutex);
@@ -586,7 +581,6 @@ static ssize_t reflash_sysfs_lockdown_show(struct file *data_file,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -597,7 +591,6 @@ static ssize_t reflash_sysfs_lockdown_store(struct file *data_file,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	pm_stay_awake(&tcm_hcd->pdev->dev);
@@ -620,7 +613,6 @@ static ssize_t reflash_sysfs_lockdown_store(struct file *data_file,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -632,7 +624,6 @@ static ssize_t reflash_sysfs_lcm_show(struct file *data_file,
 	unsigned int readlen;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	mutex_lock(&reflash_hcd->reflash_mutex);
@@ -651,7 +642,6 @@ static ssize_t reflash_sysfs_lcm_show(struct file *data_file,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -662,7 +652,6 @@ static ssize_t reflash_sysfs_lcm_store(struct file *data_file,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	pm_stay_awake(&tcm_hcd->pdev->dev);
@@ -685,7 +674,6 @@ static ssize_t reflash_sysfs_lcm_store(struct file *data_file,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -697,7 +685,6 @@ static ssize_t reflash_sysfs_oem_show(struct file *data_file,
 	unsigned int readlen;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	mutex_lock(&reflash_hcd->reflash_mutex);
@@ -716,7 +703,6 @@ static ssize_t reflash_sysfs_oem_show(struct file *data_file,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -727,7 +713,6 @@ static ssize_t reflash_sysfs_oem_store(struct file *data_file,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	pm_stay_awake(&tcm_hcd->pdev->dev);
@@ -750,7 +735,6 @@ static ssize_t reflash_sysfs_oem_store(struct file *data_file,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -760,7 +744,6 @@ static int reflash_set_up_flash_access(void)
 	unsigned int temp;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	retval = tcm_hcd->identify(tcm_hcd, true);
 	if (retval < 0) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -804,7 +787,6 @@ static int reflash_set_up_flash_access(void)
 		return -EINVAL;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -825,7 +807,6 @@ static int reflash_parse_fw_image(void)
 	const unsigned char *image;
 	const unsigned char *content;
 
-	LOG_ENTRY();
 	image = reflash_hcd->image;
 	image_info = &reflash_hcd->image_info;
 	header = (struct image_header *)image;
@@ -947,7 +928,6 @@ static int reflash_parse_fw_image(void)
 		}
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -956,7 +936,6 @@ static int reflash_get_fw_image(void)
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (reflash_hcd->image == NULL) {
 		retval = request_firmware(&reflash_hcd->fw_entry, FW_IMAGE_NAME,
 				tcm_hcd->pdev->dev.parent);
@@ -982,7 +961,6 @@ static int reflash_get_fw_image(void)
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -999,7 +977,6 @@ static enum update_area reflash_compare_id_info(void)
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 	const unsigned char *app_config_data;
 
-	LOG_ENTRY();
 	LOGV("compare id\n");
 	update_area = NONE;
 
@@ -1085,7 +1062,6 @@ static enum update_area reflash_compare_id_info(void)
 				"config only");
 	}
 
-	LOG_DONE();
 	return update_area;
 }
 
@@ -1097,7 +1073,6 @@ static int reflash_read_flash(unsigned int address, unsigned char *data,
 	unsigned int flash_addr_words;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	LOCK_BUFFER(reflash_hcd->out);
 
 	retval = syna_tcm_alloc_mem(tcm_hcd,
@@ -1163,7 +1138,6 @@ static int reflash_read_flash(unsigned int address, unsigned char *data,
 
 	UNLOCK_BUFFER(reflash_hcd->resp);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1178,7 +1152,6 @@ static int reflash_read_data(enum flash_area area, bool run_app_firmware,
 	struct syna_tcm_boot_info *boot_info;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	switch (area) {
 	case CUSTOM_LCM:
 	case CUSTOM_OEM:
@@ -1312,7 +1285,6 @@ static int reflash_read_data(enum flash_area area, bool run_app_firmware,
 	}
 
 exit:
-	LOG_DONE();
 	return retval;
 }
 
@@ -1323,7 +1295,6 @@ static int reflash_check_boot_config(void)
 	unsigned int device_addr;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (reflash_hcd->image_info.boot_config.size < BOOT_CONFIG_SIZE) {
 		LOGE(tcm_hcd->pdev->dev.parent,
 				"No valid boot config in image file\n");
@@ -1341,7 +1312,6 @@ static int reflash_check_boot_config(void)
 		return -EINVAL;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1354,7 +1324,6 @@ static int reflash_check_app_config(void)
 	unsigned int device_size;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (reflash_hcd->image_info.app_config.size == 0) {
 		LOGE(tcm_hcd->pdev->dev.parent,
 				"No application config in image file\n");
@@ -1383,7 +1352,6 @@ static int reflash_check_app_config(void)
 		return -EINVAL;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1396,7 +1364,6 @@ static int reflash_check_disp_config(void)
 	unsigned int device_size;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (reflash_hcd->image_info.disp_config.size == 0) {
 		LOGE(tcm_hcd->pdev->dev.parent,
 				"No display config in image file\n");
@@ -1424,7 +1391,6 @@ static int reflash_check_disp_config(void)
 		return -EINVAL;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1432,14 +1398,12 @@ static int reflash_check_prod_test_firmware(void)
 {
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (reflash_hcd->image_info.prod_test_firmware.size == 0) {
 		LOGE(tcm_hcd->pdev->dev.parent,
 				"No production test firmware in image file\n");
 		return -EINVAL;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1447,14 +1411,12 @@ static int reflash_check_app_firmware(void)
 {
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (reflash_hcd->image_info.app_firmware.size == 0) {
 		LOGE(tcm_hcd->pdev->dev.parent,
 				"No application firmware in image file\n");
 		return -EINVAL;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1470,7 +1432,6 @@ static int reflash_write_flash(unsigned int address, const unsigned char *data,
 	unsigned int block_address;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	w_length = tcm_hcd->wr_chunk_size - 5;
 
 	w_length = w_length - (w_length % reflash_hcd->write_block_size);
@@ -1550,7 +1511,6 @@ static int reflash_write_flash(unsigned int address, const unsigned char *data,
 	UNLOCK_BUFFER(reflash_hcd->resp);
 	UNLOCK_BUFFER(reflash_hcd->out);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1568,7 +1528,6 @@ static int reflash_erase_flash(unsigned int page_start, unsigned int page_count)
 	unsigned char out_buf[2];
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	out_buf[0] = (unsigned char)page_start;
 	out_buf[1] = (unsigned char)page_count;
 
@@ -1593,7 +1552,6 @@ static int reflash_erase_flash(unsigned int page_start, unsigned int page_count)
 
 	UNLOCK_BUFFER(reflash_hcd->resp);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1614,7 +1572,6 @@ static int reflash_update_custom_otp(const unsigned char *data,
 	unsigned int length;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	retval = reflash_set_up_flash_access();
 	if (retval < 0) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -1663,7 +1620,6 @@ static int reflash_update_custom_otp(const unsigned char *data,
 
 	tcm_hcd->update_watchdog(tcm_hcd, true);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1677,7 +1633,6 @@ static int reflash_update_custom_lcm(const unsigned char *data,
 	unsigned int page_count;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	retval = tcm_hcd->get_data_location(tcm_hcd,
 			CUSTOM_LCM,
 			&addr,
@@ -1746,7 +1701,6 @@ static int reflash_update_custom_lcm(const unsigned char *data,
 
 	tcm_hcd->update_watchdog(tcm_hcd, true);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1760,7 +1714,6 @@ static int reflash_update_custom_oem(const unsigned char *data,
 	unsigned int page_count;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	retval = tcm_hcd->get_data_location(tcm_hcd,
 			CUSTOM_OEM,
 			&addr,
@@ -1829,7 +1782,6 @@ static int reflash_update_custom_oem(const unsigned char *data,
 
 	tcm_hcd->update_watchdog(tcm_hcd, true);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1843,7 +1795,6 @@ static int reflash_update_boot_config(bool lock)
 	struct boot_config *last_slot;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	retval = reflash_set_up_flash_access();
 	if (retval < 0) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -1926,7 +1877,6 @@ static int reflash_update_boot_config(bool lock)
 
 	tcm_hcd->update_watchdog(tcm_hcd, true);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1949,7 +1899,6 @@ static int lct_syna_tp_info_update(struct syna_tcm_hcd *tcm_hcd)
 	struct app_config_header *header;
 	const unsigned char *app_config_data;
 
-	LOG_ENTRY();
 	app_config_data = reflash_hcd->image_info.app_config.data;
 	header = (struct app_config_header *)app_config_data;
 	image_config_id = header->customer_config_id;
@@ -1958,7 +1907,7 @@ static int lct_syna_tp_info_update(struct syna_tcm_hcd *tcm_hcd)
 			image_config_id[14],
 			image_config_id[15]);
 	update_lct_tp_info(tp_info_buf, NULL);
-	LOG_DONE();
+
 	return 0;
 }
 /* add tp vendor information by wanghan end */
@@ -1969,8 +1918,6 @@ static int reflash_do_reflash(void)
 	enum update_area update_area;
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
-
 	tcm_hcd->upgrading = true;
 
 	retval = reflash_get_fw_image();
@@ -2042,7 +1989,7 @@ static int reflash_do_reflash(void)
 		reflash_hcd->image_size = 0;
 	}
 	tcm_hcd->upgrading = false;
-	LOG_DONE();
+
 	return retval;
 }
 
@@ -2051,8 +1998,6 @@ static int lct_syna_tp_lockdown_info_update(char *buf, struct syna_tcm_hcd *tcm_
 {
 	int retval;
 
-	LOG_ENTRY();
-
 	mutex_lock(&tcm_hcd->extif_mutex);
 	mutex_lock(&reflash_hcd->reflash_mutex);
 
@@ -2075,7 +2020,7 @@ static int lct_syna_tp_lockdown_info_update(char *buf, struct syna_tcm_hcd *tcm_
 exit:
 	mutex_unlock(&reflash_hcd->reflash_mutex);
 	mutex_unlock(&tcm_hcd->extif_mutex);
-	LOG_DONE();
+
 	return 0;
 }
 
@@ -2091,8 +2036,6 @@ int lct_syna_tp_callback(const char *cmd)
 	char tp_lockdown_info_buf[SYNA_TP_INFO_SIZE];
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
-
 #if LCT_SYNA_TP_LOCKDOWN_CALLBACK_RUN_ONLY_ONCE
 	if (run_only_once_flag) return 0;
 #endif
@@ -2109,7 +2052,6 @@ int lct_syna_tp_callback(const char *cmd)
 	run_only_once_flag = true;
 #endif
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -2118,8 +2060,6 @@ static int lct_syna_tp_node_init(struct syna_tcm_hcd *tcm_hcd)
 	int retval;
 	char tp_info_buf[SYNA_TP_INFO_SIZE];
 
-	LOG_ENTRY();
-
 	memset(tp_info_buf, 0, sizeof(tp_info_buf));
 	sprintf(tp_info_buf, "[Vendor]boe,[FW]0x%c%c,[IC]td4320\n",
 			tcm_hcd->app_info.customer_config_id[14],
@@ -2134,7 +2074,7 @@ static int lct_syna_tp_node_init(struct syna_tcm_hcd *tcm_hcd)
 		LOGV("ERROR : lct_syna_tp_callback failed!\n");
 	}
 	set_lct_tp_info_callback(lct_syna_tp_callback);
-	LOG_DONE();
+
 	return 0;
 }
 /* add tp vendor information by wanghan end */
@@ -2148,7 +2088,6 @@ static void reflash_startup_work(struct work_struct *work)
 
 	struct syna_tcm_hcd *tcm_hcd = reflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	/* add tp vendor information by wanghan add */
 	retval = lct_syna_tp_node_init(tcm_hcd);
 	if (retval < 0){
@@ -2189,7 +2128,6 @@ static void reflash_startup_work(struct work_struct *work)
 
 	pm_relax(&tcm_hcd->pdev->dev);
 
-	LOG_DONE();
 	return;
 }
 #endif
@@ -2198,8 +2136,6 @@ static int reflash_init(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 	int idx;
-
-	LOG_ENTRY();
 	reflash_hcd = kzalloc(sizeof(*reflash_hcd), GFP_KERNEL);
 	if (!reflash_hcd) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -2280,8 +2216,6 @@ static int reflash_init(struct syna_tcm_hcd *tcm_hcd)
 	}
 
 	tcm_hcd->read_flash_data = reflash_read_data;
-
-	LOG_DONE();
 	return 0;
 
 err_custom_sysfs_create_bin_file:
@@ -2313,7 +2247,6 @@ static int reflash_init(struct syna_tcm_hcd *tcm_hcd)
 	kfree(reflash_hcd);
 	reflash_hcd = NULL;
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2321,7 +2254,6 @@ static int reflash_remove(struct syna_tcm_hcd *tcm_hcd)
 {
 	int idx;
 
-	LOG_ENTRY();
 	if (!reflash_hcd)
 		goto exit;
 
@@ -2363,7 +2295,6 @@ static int reflash_remove(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	complete(&reflash_remove_complete);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -2371,13 +2302,11 @@ static int reflash_reset(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	if (!reflash_hcd) {
 		retval = reflash_init(tcm_hcd);
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -2396,7 +2325,7 @@ static struct syna_tcm_module_cb reflash_module = {
 static int __init reflash_module_init(void)
 {
 	int retval;
-	LOG_ENTRY();
+
 	/* add check F7A LCM by wanghan start */
 	if(!lct_syna_verify_flag)
 		return -ENODEV;
@@ -2405,18 +2334,16 @@ static int __init reflash_module_init(void)
 	retval = syna_tcm_add_module(&reflash_module, true);
 	if (retval < 0)
 		LOGV("syna_tcm_add_module failed! retval = %d\n", retval);
-	LOG_DONE();
+
 	return retval;
 }
 
 static void __exit reflash_module_exit(void)
 {
-	LOG_ENTRY();
 	syna_tcm_add_module(&reflash_module, false);
 
 	wait_for_completion(&reflash_remove_complete);
 
-	LOG_DONE();
 	return;
 }
 
diff --git a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_testing.c b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_testing.c
index 4d1e6262934c..299d32253617 100644
--- a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_testing.c
+++ b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_testing.c
@@ -219,7 +219,6 @@ static ssize_t testing_sysfs_trx_trx_shorts_show(struct device *dev,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	retval = testing_trx(TEST_TRX_TRX_SHORTS);
@@ -236,7 +235,6 @@ static ssize_t testing_sysfs_trx_trx_shorts_show(struct device *dev,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -246,7 +244,6 @@ static ssize_t testing_sysfs_trx_sensor_opens_show(struct device *dev,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	retval = testing_trx(TEST_TRX_SENSOR_OPENS);
@@ -263,7 +260,6 @@ static ssize_t testing_sysfs_trx_sensor_opens_show(struct device *dev,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -273,7 +269,6 @@ static ssize_t testing_sysfs_trx_ground_shorts_show(struct device *dev,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	retval = testing_trx(TEST_TRX_GROUND_SHORTS);
@@ -290,7 +285,6 @@ static ssize_t testing_sysfs_trx_ground_shorts_show(struct device *dev,
 exit:
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -300,7 +294,6 @@ static ssize_t testing_sysfs_size_show(struct device *dev,
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	LOCK_BUFFER(testing_hcd->output);
@@ -313,7 +306,6 @@ static ssize_t testing_sysfs_size_show(struct device *dev,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -325,7 +317,6 @@ static ssize_t testing_sysfs_data_show(struct file *data_file,
 	unsigned int readlen;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	mutex_lock(&tcm_hcd->extif_mutex);
 
 	LOCK_BUFFER(testing_hcd->output);
@@ -348,7 +339,6 @@ static ssize_t testing_sysfs_data_show(struct file *data_file,
 
 	mutex_unlock(&tcm_hcd->extif_mutex);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -357,7 +347,6 @@ static int testing_run_prod_test_item(enum test_code test_code)
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (tcm_hcd->features.dual_firmware &&
 			tcm_hcd->id_info.mode != MODE_PRODUCTION_TEST) {
 		retval = tcm_hcd->switch_mode(tcm_hcd, FW_MODE_PRODUCTION_TEST);
@@ -410,7 +399,6 @@ static int testing_run_prod_test_item(enum test_code test_code)
 	UNLOCK_BUFFER(testing_hcd->resp);
 	UNLOCK_BUFFER(testing_hcd->out);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -422,7 +410,6 @@ static int testing_collect_reports(enum report_type report_type,
 	unsigned int timeout;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	testing_hcd->report_index = 0;
 	testing_hcd->report_type = report_type;
 	testing_hcd->num_of_reports = num_of_reports;
@@ -517,7 +504,6 @@ static int testing_collect_reports(enum report_type report_type,
 exit:
 	testing_hcd->report_type = 0;
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -530,7 +516,6 @@ static void testing_get_frame_size_words(unsigned int *size, bool image_only)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 
 	rows = le2_to_uint(app_info->num_of_image_rows);
@@ -546,7 +531,6 @@ static void testing_get_frame_size_words(unsigned int *size, bool image_only)
 		*size += buttons;
 	}
 
-	LOG_DONE();
 	return;
 }
 
@@ -559,7 +543,6 @@ static void testing_doze_frame_output(unsigned int rows, unsigned int cols)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 
 	header_size = 2;
@@ -611,7 +594,6 @@ static void testing_doze_frame_output(unsigned int rows, unsigned int cols)
 
 	UNLOCK_BUFFER(testing_hcd->output);
 
-	LOG_DONE();
 	return;
 }
 
@@ -624,7 +606,6 @@ static void testing_standard_frame_output(bool image_only)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 
 	testing_get_frame_size_words(&data_size, image_only);
@@ -685,7 +666,6 @@ static void testing_standard_frame_output(bool image_only)
 
 	UNLOCK_BUFFER(testing_hcd->output);
 
-	LOG_DONE();
 	return;
 }
 
@@ -705,7 +685,6 @@ static int testing_dynamic_range_doze(void)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 
 	cols = le2_to_uint(app_info->num_of_image_cols);
@@ -789,7 +768,6 @@ static int testing_dynamic_range_doze(void)
 		}
 	}
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -798,7 +776,6 @@ static int testing_dynamic_range_lpwg(void)
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	retval = tcm_hcd->set_dynamic_config(tcm_hcd,
 			DC_IN_WAKEUP_GESTURE_MODE,
 			1);
@@ -824,7 +801,6 @@ static int testing_dynamic_range_lpwg(void)
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -844,7 +820,6 @@ static int testing_dynamic_range(void)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 
 	rows = le2_to_uint(app_info->num_of_image_rows);
@@ -932,7 +907,6 @@ static int testing_dynamic_range(void)
 		}
 	}
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -952,7 +926,6 @@ static int testing_noise_doze(void)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 
 	cols = le2_to_uint(app_info->num_of_image_cols);
@@ -1021,7 +994,6 @@ static int testing_noise_doze(void)
 		}
 	}
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1030,7 +1002,6 @@ static int testing_noise_lpwg(void)
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	retval = tcm_hcd->set_dynamic_config(tcm_hcd,
 			DC_IN_WAKEUP_GESTURE_MODE,
 			1);
@@ -1056,7 +1027,6 @@ static int testing_noise_lpwg(void)
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1076,7 +1046,6 @@ static int testing_noise(void)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 
 	rows = le2_to_uint(app_info->num_of_image_rows);
@@ -1152,7 +1121,6 @@ static int testing_noise(void)
 		}
 	}
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1167,7 +1135,6 @@ static void testing_open_short_detector_output(void)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 
 	rows = le2_to_uint(app_info->num_of_image_rows);
@@ -1230,7 +1197,6 @@ static void testing_open_short_detector_output(void)
 
 	UNLOCK_BUFFER(testing_hcd->output);
 
-	LOG_DONE();
 	return;
 }
 
@@ -1248,7 +1214,6 @@ static int testing_open_short_detector(void)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 
 	rows = le2_to_uint(app_info->num_of_image_rows);
@@ -1320,7 +1285,6 @@ static int testing_open_short_detector(void)
 				"Failed to do reset\n");
 	}
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1340,7 +1304,6 @@ static int testing_pt11(void)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 
 	rows = le2_to_uint(app_info->num_of_image_rows);
@@ -1428,7 +1391,6 @@ static int testing_pt11(void)
 		}
 	}
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1448,7 +1410,6 @@ static int testing_pt12(void)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 
 	rows = le2_to_uint(app_info->num_of_image_rows);
@@ -1513,7 +1474,6 @@ static int testing_pt12(void)
 		}
 	}
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1533,7 +1493,6 @@ static int testing_pt13(void)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 
 	rows = le2_to_uint(app_info->num_of_image_rows);
@@ -1598,7 +1557,6 @@ static int testing_pt13(void)
 		}
 	}
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1608,7 +1566,6 @@ static int testing_reset_open(void)
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 
-	LOG_ENTRY();
 	if (bdata->reset_gpio < 0) {
 		LOGE(tcm_hcd->pdev->dev.parent,
 				"Hardware reset unavailable\n");
@@ -1657,7 +1614,6 @@ static int testing_reset_open(void)
 				"Failed to run application firmware\n");
 	}
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1666,7 +1622,6 @@ static void testing_lockdown_output(void)
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	LOCK_BUFFER(testing_hcd->output);
 	LOCK_BUFFER(testing_hcd->resp);
 
@@ -1699,7 +1654,6 @@ static void testing_lockdown_output(void)
 	UNLOCK_BUFFER(testing_hcd->resp);
 	UNLOCK_BUFFER(testing_hcd->output);
 
-	LOG_DONE();
 	return;
 }
 
@@ -1711,7 +1665,6 @@ static int testing_lockdown(void)
 	unsigned int limits_size;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (tcm_hcd->read_flash_data == NULL) {
 		LOGE(tcm_hcd->pdev->dev.parent,
 				"Unable to read from flash\n");
@@ -1752,7 +1705,6 @@ static int testing_lockdown(void)
 
 	testing_lockdown_output();
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1761,7 +1713,6 @@ static void testing_trx_output(void)
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	LOCK_BUFFER(testing_hcd->output);
 	LOCK_BUFFER(testing_hcd->resp);
 
@@ -1794,7 +1745,6 @@ static void testing_trx_output(void)
 	UNLOCK_BUFFER(testing_hcd->resp);
 	UNLOCK_BUFFER(testing_hcd->output);
 
-	LOG_DONE();
 	return;
 }
 
@@ -1805,7 +1755,6 @@ static int testing_trx(enum test_code test_code)
 	unsigned int idx;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	switch (test_code) {
 	case TEST_TRX_TRX_SHORTS:
 	case TEST_TRX_GROUND_SHORTS:
@@ -1850,7 +1799,6 @@ static int testing_trx(enum test_code test_code)
 		}
 	}
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1861,7 +1809,6 @@ static void testing_report(void)
 	unsigned int report_size;
 	struct syna_tcm_hcd *tcm_hcd = testing_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	report_size = tcm_hcd->report.buffer.data_length;
 
 	LOCK_BUFFER(testing_hcd->report);
@@ -1902,7 +1849,6 @@ static void testing_report(void)
 	if (testing_hcd->report_index == testing_hcd->num_of_reports)
 		complete(&report_complete);
 
-	LOG_DONE();
 	return;
 }
 
@@ -1988,7 +1934,6 @@ static int32_t lct_syna_save_rawdata_to_csv(unsigned char *buf, uint8_t x_ch, ui
 	uint32_t output_len = 0;
 	int32_t x = 0, y = 0, iArrayIndex = 0;
 
-	LOG_ENTRY();
 	fbufp = (char *)kzalloc(1024*8, GFP_KERNEL);
 	if (!fbufp) {
 		LOGV("kzalloc for fbufp failed!\n");
@@ -2036,7 +1981,7 @@ static int32_t lct_syna_save_rawdata_to_csv(unsigned char *buf, uint8_t x_ch, ui
 		kfree(fbufp);
 		fbufp = NULL;
 	}
-	LOG_DONE();
+
 	return retval;
 }
 /* add syna tp selftest by wanghan 2018-8-21 end*/
@@ -2046,7 +1991,6 @@ static int testing_init(struct syna_tcm_hcd *tcm_hcd)
 	int retval;
 	int idx;
 
-	LOG_ENTRY();
 	/* add syna tp selftest by wanghan 2018-8-21 start*/
 	lct_tp_selftest_init(lct_syna_tp_selftest);
 	/* add syna tp selftest by wanghan 2018-8-21 end*/
@@ -2093,7 +2037,6 @@ static int testing_init(struct syna_tcm_hcd *tcm_hcd)
 		goto err_sysfs_create_bin_file;
 	}
 
-	LOG_DONE();
 	return 0;
 
 err_sysfs_create_bin_file:
@@ -2113,7 +2056,6 @@ static int testing_init(struct syna_tcm_hcd *tcm_hcd)
 	kfree(testing_hcd);
 	testing_hcd = NULL;
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -2121,7 +2063,6 @@ static int testing_remove(struct syna_tcm_hcd *tcm_hcd)
 {
 	int idx;
 
-	LOG_ENTRY();
 	if (!testing_hcd)
 		goto exit;
 
@@ -2144,7 +2085,6 @@ static int testing_remove(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	complete(&testing_remove_complete);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -2152,26 +2092,22 @@ static int testing_reset(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	if (!testing_hcd) {
 		retval = testing_init(tcm_hcd);
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
 static int testing_syncbox(struct syna_tcm_hcd *tcm_hcd)
 {
-	LOG_ENTRY();
 	if (!testing_hcd)
 		return 0;
 
 	if (tcm_hcd->report.id == testing_hcd->report_type)
 		testing_report();
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -2190,7 +2126,7 @@ static struct syna_tcm_module_cb testing_module = {
 static int __init testing_module_init(void)
 {
 	int retval;
-	LOG_ENTRY();
+
 	/* add check F7A LCM by wanghan start */
 	if(!lct_syna_verify_flag)
 		return -ENODEV;
@@ -2200,18 +2136,16 @@ static int __init testing_module_init(void)
 	if(retval) {
 		LOGV("syna_tcm_add_module failed! retval = %d\n", retval);
 	}
-	LOG_DONE();
+
 	return retval;
 }
 
 static void __exit testing_module_exit(void)
 {
-	LOG_ENTRY();
 	syna_tcm_add_module(&testing_module, false);
 
 	wait_for_completion(&testing_remove_complete);
 
-	LOG_DONE();
 	return;
 }
 
diff --git a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_touch.c b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_touch.c
index 11c37d0fa6cf..b60a8f39d570 100644
--- a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_touch.c
+++ b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_touch.c
@@ -144,7 +144,6 @@ extern bool gesture_delay;
 
 int synaptics_gesture_switch(struct input_dev *dev, unsigned int type, unsigned int code, int value)
 {
-	LOG_ENTRY();
 	LOGV("type=%d, code=%d, value=%d\n", type, code, value);
 	if (type == EV_SYN && code == SYN_CONFIG)
 	{
@@ -163,7 +162,7 @@ int synaptics_gesture_switch(struct input_dev *dev, unsigned int type, unsigned
 			LOGV("enable gesture mode\n");
 		}
 	}
-	LOG_DONE();
+
 	return 0;
 }
 
@@ -178,7 +177,6 @@ static void touch_free_objects(void)
 	unsigned int idx;
 #endif
 
-	LOG_ENTRY();
 	if (touch_hcd->input_dev == NULL)
 		return;
 
@@ -202,7 +200,6 @@ static void touch_free_objects(void)
 
 	mutex_unlock(&touch_hcd->report_mutex);
 
-	LOG_DONE();
 	return;
 }
 
@@ -226,7 +223,6 @@ static int touch_get_report_data(unsigned int offset,
 	unsigned char *touch_report;
 	struct syna_tcm_hcd *tcm_hcd = touch_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (bits == 0 || bits > 32) {
 		LOGE(tcm_hcd->pdev->dev.parent,
 				"Invalid number of bits\n");
@@ -265,7 +261,6 @@ static int touch_get_report_data(unsigned int offset,
 
 	*data = output_data;
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -298,7 +293,6 @@ static int touch_parse_report(void)
 	struct syna_tcm_hcd *tcm_hcd = touch_hcd->tcm_hcd;
 	static unsigned int end_of_foreach;
 
-	LOG_ENTRY();
 	touch_data = &touch_hcd->touch_data;
 	object_data = touch_hcd->touch_data.object_data;
 
@@ -599,7 +593,6 @@ static int touch_parse_report(void)
 	}
 
 exit:
-	LOG_DONE();
 	return 0;
 }
 
@@ -623,7 +616,7 @@ static void touch_report(void)
 	struct syna_tcm_hcd *tcm_hcd = touch_hcd->tcm_hcd;
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 
-	LOG_ENTRY();
+
 	if (!touch_hcd->report_touch)
 		return;
 
@@ -727,7 +720,6 @@ static void touch_report(void)
 exit:
 	mutex_unlock(&touch_hcd->report_mutex);
 
-	LOG_DONE();
 	return;
 }
 
@@ -742,7 +734,6 @@ static int touch_set_input_params(void)
 {
 	struct syna_tcm_hcd *tcm_hcd = touch_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	input_set_abs_params(touch_hcd->input_dev,
 			ABS_MT_POSITION_X, 0, touch_hcd->max_x, 0, 0);
 	input_set_abs_params(touch_hcd->input_dev,
@@ -766,7 +757,6 @@ static int touch_set_input_params(void)
 		return -ENOMEM;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -785,7 +775,6 @@ static int touch_get_input_params(void)
 	struct syna_tcm_hcd *tcm_hcd = touch_hcd->tcm_hcd;
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 
-	LOG_ENTRY();
 	app_info = &tcm_hcd->app_info;
 	touch_hcd->max_x = le2_to_uint(app_info->max_x);
 	touch_hcd->max_y = le2_to_uint(app_info->max_y);
@@ -818,7 +807,6 @@ static int touch_get_input_params(void)
 
 	UNLOCK_BUFFER(tcm_hcd->config);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -834,7 +822,6 @@ static int touch_set_input_dev(void)
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = touch_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	touch_hcd->input_dev = input_allocate_device();
 	if (touch_hcd->input_dev == NULL) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -879,8 +866,6 @@ static int touch_set_input_dev(void)
 		touch_hcd->input_dev = NULL;
 		return retval;
 	}
-
-	LOG_DONE();
 	return 0;
 }
 
@@ -898,7 +883,6 @@ static int touch_set_report_config(void)
 	struct syna_tcm_app_info *app_info;
 	struct syna_tcm_hcd *tcm_hcd = touch_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 #ifdef USE_DEFAULT_TOUCH_REPORT_CONFIG
 	return 0;
 #endif
@@ -962,7 +946,6 @@ static int touch_set_report_config(void)
 	UNLOCK_BUFFER(touch_hcd->resp);
 	UNLOCK_BUFFER(touch_hcd->out);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -977,7 +960,6 @@ static int touch_check_input_params(void)
 	unsigned int size;
 	struct syna_tcm_hcd *tcm_hcd = touch_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (touch_hcd->max_x == 0 && touch_hcd->max_y == 0)
 		return 0;
 
@@ -1000,7 +982,6 @@ static int touch_check_input_params(void)
 	if (touch_hcd->input_params.max_y != touch_hcd->max_y)
 		return 1;
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1016,7 +997,6 @@ static int touch_set_input_reporting(void)
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = touch_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (tcm_hcd->id_info.mode != MODE_APPLICATION ||
 			tcm_hcd->app_status != APP_STATUS_OK) {
 		LOGN(tcm_hcd->pdev->dev.parent,
@@ -1072,7 +1052,6 @@ static int touch_set_input_reporting(void)
 
 	touch_hcd->report_touch = retval < 0 ? false : true;
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -1080,7 +1059,6 @@ static int touch_init(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	touch_hcd = kzalloc(sizeof(*touch_hcd), GFP_KERNEL);
 	if (!touch_hcd) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -1116,13 +1094,11 @@ static int touch_init(struct syna_tcm_hcd *tcm_hcd)
 	kfree(touch_hcd);
 	touch_hcd = NULL;
 
-	LOG_DONE();
 	return retval;
 }
 
 static int touch_remove(struct syna_tcm_hcd *tcm_hcd)
 {
-	LOG_ENTRY();
 	if (!touch_hcd)
 		goto exit;
 
@@ -1142,13 +1118,11 @@ static int touch_remove(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	complete(&touch_remove_complete);
 
-	LOG_DONE();
 	return 0;
 }
 
 static int touch_syncbox(struct syna_tcm_hcd *tcm_hcd)
 {
-	LOG_ENTRY();
 	if (!touch_hcd)
 		return 0;
 
@@ -1164,7 +1138,6 @@ static int touch_syncbox(struct syna_tcm_hcd *tcm_hcd)
 		break;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1172,7 +1145,6 @@ static int touch_asyncbox(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	if (!touch_hcd)
 		return 0;
 
@@ -1197,7 +1169,6 @@ static int touch_asyncbox(struct syna_tcm_hcd *tcm_hcd)
 		break;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1205,7 +1176,6 @@ static int touch_reset(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	if (!touch_hcd) {
 		retval = touch_init(tcm_hcd);
 		return retval;
@@ -1220,13 +1190,11 @@ static int touch_reset(struct syna_tcm_hcd *tcm_hcd)
 		}
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
 static int touch_early_suspend(struct syna_tcm_hcd *tcm_hcd)
 {
-	LOG_ENTRY();
 	if (!touch_hcd)
 		return 0;
 
@@ -1234,7 +1202,6 @@ static int touch_early_suspend(struct syna_tcm_hcd *tcm_hcd)
 
 	touch_free_objects();
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1242,7 +1209,6 @@ static int touch_suspend(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	if (!touch_hcd)
 		return 0;
 
@@ -1267,7 +1233,6 @@ static int touch_suspend(struct syna_tcm_hcd *tcm_hcd)
 		}
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1275,7 +1240,6 @@ static int touch_resume(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	if (!touch_hcd)
 		return 0;
 
@@ -1297,7 +1261,6 @@ static int touch_resume(struct syna_tcm_hcd *tcm_hcd)
 		}
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1316,7 +1279,7 @@ static struct syna_tcm_module_cb touch_module = {
 static int __init touch_module_init(void)
 {
 	int retval;
-	LOG_ENTRY();
+
 	/* add check F7A LCM by wanghan start */
 	if(!lct_syna_verify_flag)
 		return -ENODEV;
@@ -1326,18 +1289,16 @@ static int __init touch_module_init(void)
 	if(retval) {
 		LOGV("syna_tcm_add_module failed! retval = %d\n", retval);
 	}
-	LOG_DONE();
+
 	return retval;
 }
 
 static void __exit touch_module_exit(void)
 {
-	LOG_ENTRY();
 	syna_tcm_add_module(&touch_module, false);
 
 	wait_for_completion(&touch_remove_complete);
 
-	LOG_DONE();
 	return;
 }
 
diff --git a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_zeroflash.c b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_zeroflash.c
index 16719e1ab2b3..beb7e44b3ac3 100644
--- a/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_zeroflash.c
+++ b/drivers/input/touchscreen/synaptics_tcm_f7a/synaptics_tcm_zeroflash.c
@@ -198,7 +198,6 @@ static int zeroflash_check_uboot(void)
 	struct rmi_pdt_entry p_entry;
 	struct syna_tcm_hcd *tcm_hcd = zeroflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	retval = syna_tcm_rmi_read(tcm_hcd,
 			PDT_END_ADDR,
 			&fn_number,
@@ -258,7 +257,6 @@ static int zeroflash_check_uboot(void)
 		return -ENODEV;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -279,7 +277,6 @@ static int zeroflash_parse_fw_image(void)
 	const unsigned char *image;
 	const unsigned char *content;
 
-	LOG_ENTRY();
 	image = zeroflash_hcd->image;
 	image_info = &zeroflash_hcd->image_info;
 	header = (struct image_header *)image;
@@ -382,7 +379,6 @@ static int zeroflash_parse_fw_image(void)
 		}
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -391,7 +387,6 @@ static int zeroflash_get_fw_image(void)
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = zeroflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	if (zeroflash_hcd->fw_entry != NULL)
 		return 0;
 
@@ -425,7 +420,6 @@ static int zeroflash_get_fw_image(void)
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -434,7 +428,6 @@ static void zeroflash_download_config(void)
 	struct firmware_status *fw_status;
 	struct syna_tcm_hcd *tcm_hcd = zeroflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	fw_status = &zeroflash_hcd->fw_status;
 
 	if (!fw_status->need_app_config && !fw_status->need_disp_config) {
@@ -451,15 +444,12 @@ static void zeroflash_download_config(void)
 
 	queue_work(zeroflash_hcd->workqueue, &zeroflash_hcd->config_work);
 
-	LOG_DONE();
 	return;
 }
 
 static void zeroflash_download_firmware(void)
 {
-	LOG_ENTRY();
 	queue_work(zeroflash_hcd->workqueue, &zeroflash_hcd->firmware_work);
-	LOG_DONE();
 
 	return;
 }
@@ -472,7 +462,6 @@ static int zeroflash_download_disp_config(void)
 	struct syna_tcm_hcd *tcm_hcd = zeroflash_hcd->tcm_hcd;
 	static unsigned int retry_count;
 
-	LOG_ENTRY();
 	LOGN(tcm_hcd->pdev->dev.parent,
 			"Downloading display config\n");
 
@@ -571,7 +560,6 @@ static int zeroflash_download_disp_config(void)
 unlock_out:
 	UNLOCK_BUFFER(zeroflash_hcd->out);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -584,7 +572,6 @@ static int zeroflash_download_app_config(void)
 	struct syna_tcm_hcd *tcm_hcd = zeroflash_hcd->tcm_hcd;
 	static unsigned int retry_count;
 
-	LOG_ENTRY();
 	LOGN(tcm_hcd->pdev->dev.parent,
 			"Downloading application config\n");
 
@@ -688,7 +675,6 @@ static int zeroflash_download_app_config(void)
 unlock_out:
 	UNLOCK_BUFFER(zeroflash_hcd->out);
 
-	LOG_DONE();
 	return retval;
 }
 
@@ -697,7 +683,6 @@ static void zeroflash_download_config_work(struct work_struct *work)
 	int retval;
 	struct syna_tcm_hcd *tcm_hcd = zeroflash_hcd->tcm_hcd;
 
-	LOG_ENTRY();
 	retval = zeroflash_get_fw_image();
 	if (retval < 0) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -734,7 +719,6 @@ static void zeroflash_download_config_work(struct work_struct *work)
 
 	zeroflash_download_config();
 
-	LOG_DONE();
 	return;
 }
 
@@ -748,7 +732,6 @@ static int zeroflash_download_app_fw(void)
 	const struct syna_tcm_board_data *bdata = tcm_hcd->hw_if->bdata;
 #endif
 
-	LOG_ENTRY();
 	LOGN(tcm_hcd->pdev->dev.parent,
 			"Downloading application firmware\n");
 
@@ -822,7 +805,6 @@ static int zeroflash_download_app_fw(void)
 	LOGN(tcm_hcd->pdev->dev.parent,
 			"Application firmware downloaded\n");
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -833,7 +815,6 @@ static void zeroflash_download_firmware_work(struct work_struct *work)
 	struct syna_tcm_hcd *tcm_hcd = zeroflash_hcd->tcm_hcd;
 	static unsigned int retry_count;
 
-	LOG_ENTRY();
 	retval = zeroflash_check_uboot();
 	if (retval < 0) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -905,13 +886,11 @@ static void zeroflash_download_firmware_work(struct work_struct *work)
 		}
 	}
 
-	LOG_DONE();
 	return;
 }
 
 static int zeroflash_init(struct syna_tcm_hcd *tcm_hcd)
 {
-	LOG_ENTRY();
 	zeroflash_hcd = kzalloc(sizeof(*zeroflash_hcd), GFP_KERNEL);
 	if (!zeroflash_hcd) {
 		LOGE(tcm_hcd->pdev->dev.parent,
@@ -935,13 +914,11 @@ static int zeroflash_init(struct syna_tcm_hcd *tcm_hcd)
 			tcm_hcd->hw_if->bus_io->type == BUS_SPI)
 		zeroflash_download_firmware();
 
-	LOG_DONE();
 	return 0;
 }
 
 static int zeroflash_remove(struct syna_tcm_hcd *tcm_hcd)
 {
-	LOG_ENTRY();
 	if (!zeroflash_hcd)
 		goto exit;
 
@@ -962,7 +939,6 @@ static int zeroflash_remove(struct syna_tcm_hcd *tcm_hcd)
 exit:
 	complete(&zeroflash_remove_complete);
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -971,7 +947,6 @@ static int zeroflash_syncbox(struct syna_tcm_hcd *tcm_hcd)
 	int retval;
 	unsigned char *fw_status;
 
-	LOG_ENTRY();
 	if (!zeroflash_hcd)
 		return 0;
 
@@ -1003,7 +978,6 @@ static int zeroflash_syncbox(struct syna_tcm_hcd *tcm_hcd)
 		break;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1011,13 +985,11 @@ static int zeroflash_reset(struct syna_tcm_hcd *tcm_hcd)
 {
 	int retval;
 
-	LOG_ENTRY();
 	if (!zeroflash_hcd) {
 		retval = zeroflash_init(tcm_hcd);
 		return retval;
 	}
 
-	LOG_DONE();
 	return 0;
 }
 
@@ -1036,7 +1008,7 @@ static struct syna_tcm_module_cb zeroflash_module = {
 static int __init zeroflash_module_init(void)
 {
 	int retval;
-	LOG_ENTRY();
+
 	/* add check F7A LCM by wanghan start */
 	if(!lct_syna_verify_flag)
 		return -ENODEV;
@@ -1046,18 +1018,16 @@ static int __init zeroflash_module_init(void)
 	if(retval) {
 		LOGV("syna_tcm_add_module failed! retval = %d\n", retval);
 	}
-	LOG_DONE();
+
 	return retval;
 }
 
 static void __exit zeroflash_module_exit(void)
 {
-	LOG_ENTRY();
 	syna_tcm_add_module(&zeroflash_module, false);
 
 	wait_for_completion(&zeroflash_remove_complete);
 
-	LOG_DONE();
 	return;
 }
 
-- 
2.49.0

